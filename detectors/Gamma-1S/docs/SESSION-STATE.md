# Состояние линии Гамма-1С на 31.07.2026

Снимок перед перезапуском сессии. Дерево чисто, HEAD `6be2d27`.

## Что идёт прямо сейчас

**Регенерация Маринелли-ОИСН16 (`rho1.00`/`rho1.60`), в фоне.** Пять из семи
наборов (water1.00, denta0.60/1.60, petri0.60/1.60) — все 120 точек готовы,
вердикт `ok`, отпечаток `bd17f9bea1e4`. Маринелли считается отдельным
драйвером `run_grid.py OISN16` (пробел покрытия `run_all_grids.py` найден и
задокументирован в прошлом снимке) — на момент этой правки 21/48 точек.
Прежние 48 точек лежат в `<build>/pre65977db/grid/`, откат обратим
перемещением.

Пока `rho1.00`/`rho1.60` не досчитаны, `results/geometry_response.csv` и §5.2
отчёта стоят на входах с вердиктом `unstamped`; это объявлено в самом разделе.

## Входы

| Набор | Состояние | Отпечаток |
|---|---|---|
| точечная сетка `p5cm` | 24/24 | `bd17f9bea1e4`, вердикт `ok` |
| точечная сетка `p25cm` | 24/24 | `bd17f9bea1e4`, вердикт `ok` |
| прогоны распада `p5_*` | 28/28 | `bd17f9bea1e4`, вердикт `ok` |
| объёмные сетки | 69/216, идёт счёт | будет `bd17f9bea1e4` |

Отпечаток дерева исходников Гамма-1С — `bd17f9bea1e4`, RadiaCode-103 —
`09979e68b48d`.

## Действующие числа

Единое правило съёма на обеих сторонах, входы с вердиктом `ok`:

| Геометрия | Линий | Расчёт / паспорт | χ²/ν |
|---|---|---|---|
| точечная 5 см | 21 | 1,0365 ± 0,0112 | 1,79 |
| точечная 25 см | 7 | 1,0281 ± 0,0238 | 2,59 |

Суженный набор (обе группы на одних линиях): расчёт 1,0376 ± 0,0131 против
аттестации 0,9614 ± 0,0277 на 5 см; 1,0197 ± 0,0301 против 0,9562 ± 0,0150 на
25 см.

## Закрыто в этой сессии

1. **Штамп провенанса** из бинарника: `common/cmake/provenance.cmake` считает
   SHA1 по исходникам перед каждой сборкой, `main.cc` печатает его, аргументы
   прогона и долю телесного угла в шапку каждого спектра. Пять вердиктов,
   сторож `tools/check_stamp.py`. Подключён и к RadiaCode-103.
2. **«Третьей цепочки» не было** — в дереве лежал не перегенерированный выход
   той же цепочки. Правило: устаревший ВЫХОД скрипта неотличим от результата
   другого метода.
3. **Окно съёма площади** сведено в `common/py/peakwin.py`, задаётся в каналах,
   край подложки `E−25`. Прежде правило жило в четырёх местах.
4. **Задача 135**: вырез полос вылета отключён как неисправный (съедал подложку
   измеренного спектра), заражение объявлено систематикой с выводимой полосой
   54,3…217,4 кэВ.
5. **Параметры прибора** сведены в `analysis/detector_params.py` — `FWHM662` и
   `ESCAPES` стояли копиями и разошлись.
6. **Отчёт** `docs/report.md` написан и правлен по независимому аудиту (три P0).
7. **Задача 133, основная часть** (после перезапуска, коммиты `ebed679`,
   `8a83155`, `1902708`, `6be2d27`): `mucalc.cc`/`wallfield.cc` получили
   собственные отпечатки (`G1SMU`/`RCMU`/`RCWF`) вместо того, чтобы либо
   печатать чужой хеш (mucalc обоих детекторов), либо не иметь провенанса
   вовсе (wallfield). Два новых теста: `test_stamp_cmake_match.py` (python
   `source_sha1` совпадает с `cmake -P provenance.cmake` побайтово на реальном
   дереве, все 5 наборов) и `test_stamp_coverage.py` (каждый `*.cc`/`*.hh` в
   `geometry/` входит хотя бы в один отпечаток — 4/4 и 5/5). `git_describe`
   (и cmake-, и python-сторона) теперь видит untracked-файлы, которых
   `--dirty` не замечает.

## Открытое, в порядке очереди

1. **Дождаться объёмных сеток**, пересчитать `geometry_response.py` и §5.2
   отчёта; снять оговорку про `unstamped`.
2. **Повторный аудит отчёта** субагентом. Первый круг дал три P0 — одного круга
   мало. Отчёт наружу до этого не идёт.
3. **Задача 132**: `kit_recalc_point.csv` смешивает две наблюдаемые по столбцу
   геометрии (5 см — размытый спектр и доли ПШПВ, 25 см — депозит и кэВ).
   Согласие двух дистанций сейчас есть согласие двух конвенций. Замечено при
   разборе: измеренная сторона (`bm.net_rate`, окно ~1 ПШПВ) считается ОДНИМ
   вызовом для обеих геометрий, а модельная — РАЗНЫМИ (5 см размыта+ПШПВ-окно,
   25 см депозит+окно в каналах); унификация меняла бы заголовочные числа
   отчёта §5.1 — без операторского решения не трогать.
4. **Задача 133, остаток** (не путать с закрытым выше): (а) `peakwin.channels`
   не зажат сверху числом реальных каналов сетки — при E >~3694,5 кэВ считал
   бы несуществующие каналы; текущей сеткой недостижимо, латентно; (б)
   гарантия «отпечаток не отстанет от бинарника» проверена только на Ninja,
   тест требует отдельной сборки — отложено до завершения фоновой регенерации
   (не конкурировать за диск/процесс); (в) `PASSPORT` в `point_recalc.py`
   публикует активности/даты 25 источников комплекта — квазиидентификатор,
   решение за оператором.
5. **Долг штампа**: шесть таблиц из шестидесяти в `results/` имеют объявление
   наблюдаемой.
6. **Задача 95** — бюджет размерных неопределённостей; чувствительности 3,05 и
   0,89 %/мм производящего скрипта не имеют и подлежат пересчёту.

## Лид из соседнего контура (не проверен)

В корпусе `gamma-spectrum-analysis` появились методические документы ЛСРМ
SpectraLineXX 1.7 с числовыми порогами. Применимо: порог площади пика
≥ 20 000 импульсов на устойчивость формы — прямая проверка достаточности
статистики модельных пиков. Неприменимо без разбора: `χ²/ν ≈ 1` у ЛСРМ есть
критерий подгонки ФОРМЫ ЛИНИИ, а в §5.1 отчёта тем же именем названа мера
согласия рядов нуклидов между собой — разные величины. Пороги пересказаны, не
прочитаны; перед использованием читать первоисточник.

## Ограничения, действующие всегда

Заводской номер детектора, номера источников и фамилии операторов в публичное
дерево не идут; `tools/check_paths.py` прогоняется перед каждым коммитом. Пуш в
origin — только по явной команде оператора; сейчас непушенных коммитов около
трёх десятков.
