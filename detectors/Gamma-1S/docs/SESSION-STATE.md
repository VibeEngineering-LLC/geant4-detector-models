# Состояние линии Гамма-1С на 31.07.2026

Снимок. Дерево чисто, HEAD `475b300` + правки по итогам аудита задачи 132
(ещё не закоммичены на момент записи снимка — см. незакоммиченные изменения).

## Входы

Все сетки Гамма-1С — вердикт `ok`, отпечаток `bd17f9bea1e4`.

| Набор | Состояние |
|---|---|
| точечная сетка `p5cm` / `p25cm` | 24/24 каждая |
| прогоны распада `p5_*` | 28/28 (случайный розыгрыш — см. задачу 136) |
| объёмные сетки (water1.00, denta0.60/1.60, petri0.60/1.60, rho1.00/1.60) | 216/216, все семь наборов |

Отпечаток дерева исходников RadiaCode-103 — `09979e68b48d`.

## Действующие числа

Единое правило съёма на обеих сторонах (5 см и 25 см — ОДНА конвенция с
задачи 132, см. ниже), входы с вердиктом `ok`:

| Геометрия | Линий | Расчёт / паспорт | χ²/ν |
|---|---|---|---|
| точечная 5 см | 21 | 1,0365 ± 0,0112 | 1,79 |
| точечная 25 см | 7 | 1,0381 ± 0,0214 | 2,10 |

Суженный набор (обе группы на одних линиях): расчёт 1,0376 ± 0,0131 против
аттестации 0,9614 ± 0,0277 на 5 см; 1,0248 ± 0,0192 против 0,9562 ± 0,0150 на
25 см.

Отклик на геометрию (§5.2 отчёта, полный пересчёт по всем семи сеткам):
согласие с ЛСРМ тем лучше, чем дальше шаг от точечной геометрии — 0,5 σ на
Маринелли/Дента, 4,1 σ на Дента/Петри, 13,5 σ на Петри/точка. Антисовпадения
знака отклика — только при несопоставимой плотности пробы.

## Закрыто в этой сессии

1. **Штамп провенанса** из бинарника; пять вердиктов, сторож
   `tools/check_stamp.py`. Подключён и к RadiaCode-103.
2. **«Третьей цепочки» не было** — устаревший ВЫХОД скрипта неотличим от
   результата другого метода.
3. **Окно съёма площади** сведено в `common/py/peakwin.py`.
4. **Задача 135**: вырез полос вылета отключён как неисправный, заражение
   объявлено систематикой (54,3…217,4 кэВ).
5. **Параметры прибора** сведены в `analysis/detector_params.py`.
6. **Задача 133, основная часть**: `mucalc.cc`/`wallfield.cc` получили
   собственные отпечатки; два новых теста провенанса; `git_describe` видит
   untracked-файлы.
7. **Маринелли-ОИСН16 досчитана** (48/48) — `geometry_response.py` пересчитан
   по полному набору из семи `ok`-сеток; `geometry-response.md` переписан
   целиком (прежняя редакция стояла не только на неполном Маринелли, но и на
   несвежих Петри/Дента).
8. **Второй независимый аудит `report.md`** (после Маринелли): 2 P0 + 3 P1,
   все закрыты — ошибка «семь сеток против списка на восемь» в
   geometry-response.md; отзыв непрослеженного «дефицита телесного угла»;
   ba133_sumpeak.py получил штамп (пересчёт дал 107 вместо процитированных
   97 — вход перегенерировался случайным розыгрышем без синхронного
   пересчёта таблицы, см. задачу 136); оговорка к чувствительностям §5.4;
   README поправлены на актуальный отчёт.
9. **Задача 95**: оператор подтвердил плоскость отсчёта 5/25 см — наружная
   поверхность корпуса (не кристалл). `distance_sensitivity.py` пересчитан с
   верным сдвигом 9,5 мм: 2,80/0,76 %/мм, отношение 3,67 (было 3,12/0,79/3,95
   по ошибке в первой версии скрипта). Ближе к унаследованному 3,43 — как и
   ожидалось при верной точке отсчёта. Допуск для недокументированных
   размерных величин — рабочий интервал ±10 % от номинала (решение
   оператора), сам бюджет прогонами ещё не считался.
10. **Задача 133(в)**: паспорт комплекта (активности/даты поверки 25
    источников) вынесен из `point_recalc.py` во внешний JSON — квазиидентификатор
    комплекта, решение оператора «обезличить». Путь только через
    `G4MODELS_PASSPORT_GAMMA_1S`, закоммиченного отката нет.
11. **Задача 132**: конвенции съёма площади 5 см и 25 см сведены к одной
    (решение оператора «да»). `eps_mono_point` (25 см) теперь тоже размывает
    сетку моноэнергий и снимает площадь ПШПВ-окном — как 5 см и как
    измеренная сторона. 25 см сдвинулась с 1,0281±0,0238 (χ²/ν=2,59) на
    1,0381±0,0214 (χ²/ν=2,10) — ближе к 5 см, внутренне согласнее. Побочно
    исправлен `tcc_selfcheck.py` (использовал ту же функцию для другого,
    narrow-window по замыслу, контроля воспроизводимости — переведён на
    `mono_with_err` с обеих сторон).
12. **Третий независимый аудит** (на задачу 132, коммит `475b300`): нашёл 1 P0
    (этот самый файл несколько часов оставался с ДОПОЛУНИФИКАЦИОННЫМИ числами
    под заголовком «единое правило» — переписан) и 1 P1 (побочный сдвиг
    поправки на суммирование `C5/C25` не был раскрыт — добавлена оговорка в
    report.md §4 п.7). P2-находки (обоснование асимметрии `dirty_shelves`,
    понижение источника `solid_angle_frac` в `tcc_selfcheck.py` на p5cm,
    коллизия номера задачи «132» с более ранней мелкой правкой в
    `shelf_window_effect.py`) — первая задокументирована комментарием в коде,
    остальные две оставлены как есть (низкое влияние, не искажают
    опубликованные числа).

## Открытое, в порядке очереди

1. **Задача 133, остаток**: (а) `peakwin.channels` не зажат сверху числом
   реальных каналов сетки — латентно, недостижимо текущей сеткой; (б)
   гарантия «отпечаток не отстанет от бинарника» проверена только на Ninja,
   тест требует отдельной сборки в чистый каталог (build свободен, можно
   сделать).
2. **Долг штампа**: точный остаток таблиц `results/` без объявления
   наблюдаемой не пересчитан после сегодняшних правок.
3. **Задача 95, остаток**: сам бюджет размерных неопределённостей — нужны
   прогоны со сдвигом уровня засыпки/плотности на ±10 % на 3-4 мягких
   энергиях + 662 контроль; источники для обмера недоступны (оператор,
   28.07), бюджет только расчётный.
4. **Задача 136**: механизм — src_sha1 не различает случайные розыгрыши МК
   одного и того же кода. Сегодняшний обзвон (см. история задачи) нашёл живой
   экземпляр только в ba133_sumpeak.py (исправлен); сам механизм дыры не
   закрыт, четыре варианта починки не выбраны.
5. **Коллизия номера задачи «132»** (найдено третьим аудитом, не мой формат
   нумерации): `shelf_window_effect.py` использует «задача 132» для другой,
   более ранней и мелкой правки (сдвиг края полки E-30→E-25), не для
   унификации конвенций 5/25 см. В репозитории нет внутреннего реестра задач
   для разрешения — стоит внимания оператора, если нумерация важна.

## Лид из соседнего контура (не проверен)

В корпусе `gamma-spectrum-analysis` появились методические документы ЛСРМ
SpectraLineXX 1.7 с числовыми порогами. Применимо: порог площади пика
≥ 20 000 импульсов на устойчивость формы. Неприменимо без разбора: `χ²/ν ≈ 1`
у ЛСРМ — критерий подгонки ФОРМЫ ЛИНИИ, а в §5.1 отчёта тем же именем названа
мера согласия рядов нуклидов между собой — разные величины. Пороги
пересказаны, не прочитаны; перед использованием читать первоисточник.

## Ограничения, действующие всегда

Заводской номер детектора, номера источников и фамилии операторов в публичное
дерево не идут; `tools/check_paths.py` прогоняется перед каждым коммитом. Пуш в
origin — только по явной команде оператора; сейчас непушенных коммитов больше
трёх десятков.
