# Замечания оператора по странице Ra-226

Копятся здесь по мере поступления (см. правило `operator-remarks-inventory`
в памяти контура), применяются сразу же по ходу сессии. Пока замечание не
закрыто — остаётся в списке с датой и состоянием. Цель — не повторить те же
провалы (незамеченный баг, недоделанный элемент) на следующем такого же рода
развороте страницы (Гамма-1С по другим нуклидам, другие детекторы).

## 1. Калибровочная вкладка — взять за основу референс Th-232 (09-10.08.2026) — ЗАКРЫТО

**Замечание:** «делай вебку выгружай на гит. Возми за основу вебку по
торию. Обрати внимание на окно калибровки. Должно быть один к одному с
референсом».

Прежняя вкладка «калибровка» была собственной упрощённой отрисовкой (сырые
отсчёты без вычета фона, без переключателей, без зума, без наведения на
реперы, без деконволюции ПШПВ по точкам). Заменена на код, идентичный
`g1s-th232.js` (drawCal/wireCal/drawFwhm) — переключатели
образец/фон/разность/лог-шкала/реперы, зум протяжкой мыши, наведение с
тултипом, таблица параметров калибровки, таблица опорных линий ПШПВ,
график степенного закона. `export_ra226_data.py` дополнен
`meta.cal_sample/cal_bg` и полным словарём `fit_fwhm_calibration`.
Задеплоено в `docs/gamma-1s-ra226/`. Коммит `b2f91e8`.

**Урок, вскрывшийся ПОЗЖЕ (см. №4):** «проверено локальным http.server,
консоль чистая» на момент коммита была неполной проверкой — сверялось
«не пусто ли» по неверному эталону цвета фона канвы (`clearRect` не красит
канву в цвет бумаги, канва прозрачна) вместо проверки, что САМИ линии
графика нарисованы. Свежая канва тоже даёт «не белый» результат при таком
сравнении. На следующий раз: проверять конкретные пиксели ожидаемого цвета
трассы (`#16140f` образец, `#0f5aa8` фон), а не факт отличия от
предполагаемого фона.

## 2. Легенда — снять disabled, убрать «(γ нет)» (10.08.2026) — ЗАКРЫТО

**Замечание:** «разморозь кнопки. убери надпись - гаммы нет».

Чекбоксы инертных нуклидов (Rn-222/Po-218/Po-214 — вклад в модель точный
ноль) были задизейблены; разморожены. Подпись «(γ нет)» в чипе легенды
убрана — приглушённая `opacity:0.45` и тултип при наведении остались
единственным визуальным признаком. Коммит `c541663`.

## 3. Диапазон энергий графиков 30-3000 кэВ (10.08.2026) — ЗАКРЫТО

**Замечание:** «диапазон энергий 30-3000».

Ось X графиков `cvM2` (метод 2) и `cvCal` (калибровка) была `0` до
`e[e.length-1]` (динамически, ≈3117 кэВ). Зафиксирована на 30-3000 кэВ
явно (`drawSpectrum`, `attachCursorM2`, `drawCal`/`calEfromX`/
`calRefLineAt` — четыре места). Проверено наведением курсора у обоих
краёв канвы: слева 31 кэВ, справа 3001 кэВ.

## 4. Проверка утечки радона (10.08.2026) — ЗАКРЫТО (число ⚠️ не подтверждено)

**Замечание:** «добавь проверку на учетчку радона».

Прежняя попытка (09.08.2026, `radon_check` в `export_ra226_data.py`) была
локальной (`peak_area_with_shelf` в узком окне вокруг 186 кэВ) и провалилась
содержательно — чистая площадь отрицательна из-за контаминации соседней
линией 241,995 кэВ Pb-214.

Новая реализация (10.08.2026): деконволюция ПО ВСЕМУ спектру двумя
независимыми амплитудами методом 2 (родитель Ra-226 против дочерних
Rn-222+Pb-214+Bi-214+Po-218/214), теми же базисами `by_nuc_w`, что и
одноамплитудная модель. Число получено и СТАБИЛЬНО к границе окна подгонки
(проверено вручную на 400/700/1200/2300 кэВ: отношение 0,47-0,57, не
порядковый разброс) — не численный артефакт обусловленности как таковой.
НО физически неправдоподобно: A(родитель) ≈ вдвое БОЛЬШЕ паспортной
активности, а не на доли процента ниже, как ожидалось бы при частичной
потере Rn-222. Родительская сторона держится на ЕДИНСТВЕННОЙ линии 186,211
кэВ (I=3,565 %) — правдоподобнее объясняется несмоделированным вкладом
(гипотеза, НЕ подтверждена: линия U-235 185,715 кэВ, I=57,2 %, неразличима
с Ra-226 186,211 при разрешении NaI ≈22,6 кэВ), чем реальным дисбалансом
цепочки. Помечено `reliable: false` в JSON.

Отображено на вкладке «сравнение» (`radonNote`) с итоговыми числами:
A(родитель)=6046±231 Бк, A(дочерние)=2852±12 Бк, отношение=0,472±0,018,
явной пометкой ⚠️ «посчитано, не подтверждено» и краткой формулировкой
альтернативной гипотезы (несмоделированный вклад, кандидат — U-235
185,715 кэВ).

## 5-1. Спектр метода 2 не интерактивен (10.08.2026) — ЗАКРЫТО

**Замечание:** «спектр с разложением не интерактивной - мышь не
определяет энергию. масштаб не меняется».

`cvM2` не имел наведения курсора (`cursorM2` — статичный текст) в отличие
от референса Th-232 (`cursorText`/`attachCursor` на `cvM1`/`cvM2`).
Портирован `cursorTextM2()`/`attachCursorM2()` один в один: наведение
показывает энергию/измерено-без-фона/модель/топ-4 вклада нуклидов,
тултип у курсора. Проверено на обеих границах диапазона (см. №3) —
работает. Насчёт «масштаба»: в референсе Th-232 у вкладок метода 1/2
ЗУМА НЕТ ВООБЩЕ (только у калибровки) — значит и здесь не добавлялся,
чтобы не разойтись с «один в один».

## 5-2. Вопрос по пикам 2200-2450 кэВ, не покрытым моделью (10.08.2026) — СПРАВОЧНО

**Замечание (со скриншотом хвоста спектра):** «тут что за пики? почему
не покрывается шаблоном?»

Не баг, разобрано по данным. Горб у 2200-2250 кэВ — линия Bi-214
2204,100 кэВ (I=4,92 %), она в модели (даже в отобранной библиотеке)
есть. Дальше модель падает в ноль: (1) `E_FIT_HI=2300` кэВ — подгонка
дальше не оптимизируется под данные; (2) в отобранной библиотеке (вкл.
по умолчанию) последняя линия — как раз 2204,1 кэВ; реальная линия
Bi-214 2447,69 кэВ (I=1,545 %) есть в полной библиотеке, но ниже порога
2 % отбора. Дребезг чёрной кривой там — пуассоновский шум на 36-220
отсч/канал (фон там уже 2-4 отсч, не источник шума).

## 5-3. Шкала графика сравнения — привести к референсу (10.08.2026) — ЗАКРЫТО

**Замечание:** «шкалу сравнений сделай как на референсе».

Прежний `fillCompare()` рисовал закрашенные прямоугольники без рамки/
сетки/оси. Портирован `drawCmp()` из `g1s-th232.js` один в один: рамка
`strokeRect`, сетка по X с подписями делений, подпись оси «активность,
Бк», метка жирным над отметкой, значение моноширинным шрифтом рядом.
Высота канвы `cvCmp` приведена к референсу (200 вместо 160).

## 5. Калибровочный график не отрисовывает трассы (10.08.2026) — ОТКРЫТО

**Замечание:** «калибровка не отобразилась» (со скриншотом: сетка, оси,
реперные флажки на месте, линии образца/фона внутри графика ОТСУТСТВУЮТ).

Разбирается прямо сейчас. См. урок в №1 про негодную предыдущую
самопроверку. Задача заводится следующим сообщением в трекере.

## 6. Затенение чипов легенды (10.08.2026) — ЗАКРЫТО

**Замечание:** «затенение кнопок убери».

`chip.style.opacity = "0.45"` для инертных нуклидов убрано (тултип при
наведении остался). Продолжение №2 выше (задача #4 в трекере).

## 7. Баг makeY() — калибровочная трасса не рисуется (10.08.2026) — ЗАКРЫТО

**Замечание:** «калибровка не отобразилась» (скриншот: сетка/оси/реперы
есть, трассы образец/фон отсутствуют, подписи оси Y тоже отсутствуют).

**Причина найдена прямым чтением кода:** `makeY()` в `ra226.js` (своя,
локальная версия для трёх старых функций отрисовки) возвращала только
`{map: fn}`, без полей `.lo`/`.hi`. Портированный `drawCal()` (взят из
`g1s-th232.js` один в один) использует `Y.lo` и `Y.hi` — при их
отсутствии `Math.max(v, undefined)` даёт `NaN`, и `g.lineTo(x, NaN)`
canvas молча пропускает — трасса невидима. По той же причине пустым
оказался список делений оси Y в лог-режиме (`Math.ceil(Math.log10(undefined))`
= `NaN`, цикл `for(d=0;d<=NaN;d++)` не выполняется ни разу).

**Фикс:** `makeY()` приведена к виду `g1s-th232.js` — возвращает `lo`,
`hi`, `log` вместе с `map`. Проверено точечным пиксельным тестом (не тем
методом, что дал ложное «не пусто» при первой публикации №1) — найдено
5626 пикселей цвета трассы фона и 303 пикселя подписей делений оси слева.

## 5-4. Пики 2600 кэВ — внешняя проверка (10.08.2026) — ЗАКРЫТО (справочно)

**Замечание:** «проведи поиск в сети по линия радия или линиям суммирования
на 2600», позже «фиксируй, применяй».

Прямая проверка `data/ensdf_ra226_chain_lines.csv` (IAEA-NDS/ENSDF, curl
09.08.2026 — ✅): у Bi-214 линии реально есть до ~3183–3269 кэВ (оператор
подтвердил цифрой «до 3183,57» — совпадает), но в районе 2600 кэВ они на
3–5 порядков слабее сильных (2604,5 I=0,00041 %, 2630,9 I=0,00086 % —
против 2204,1 I=4,92 %, 2447,7 I=1,545 %) — ожидаемый вклад для такой
интенсивности пренебрежимо мал против наблюдаемых ~50–60 отсчётов.
Веб-поиск (RU+EN) не дал ни одного источника с линией/суммированием на
2600 кэВ в цепочке Ra-226. Вывод: бугорок у 2600–2620 кэВ — статистический
шум, не физическая линия. Факт зафиксирован в корпус `sci-search`
(`verified-facts.jsonl`, claim «Bi-214 gamma lines near 2600 keV...»).

## 5-5. ХРИ свинца защиты — на Ra-226 не оговорено (10.08.2026) — В РАБОТЕ

**Замечание:** «у нас есть свинец защиты. Активность образца вызывает
отклик ХРИ свинца. В референсе есть».

На Th-232 (`t-method` попап, раздел «Что в эту сущность НЕ входит»)
явно написано: характеристический рентген свинца защиты (Kα≈73–75,
Kβ≈85–87 кэВ) физически присутствует в прогоне (защита — сплошной
объём свинца, атомная релаксация включена в физсписке), но НЕ
выделяется отдельной сущностью — выход Geant4 не несёт метку
объёма/процесса рождения кванта. У метода 2 (строится от эмиссии ядра,
не от объёма) вклад защиты не учитывается вовсе. Ra-226 использует ТУ
ЖЕ модель/сетку/защиту (переиспользована без нового прогона) — то же
ограничение применимо, но у лёгкой страницы Ra-226 нет попапов вообще,
и оговорки нигде нет. Добавляется короткий эквивалент абзаца.

## 5-6. Метод 1 (МК-шаблоны по нуклидам) — полный прогон (10.08.2026) — ЗАКРЫТО

**Замечание:** «у нас есть свинец защиты. Активность образца вызывает
отклик ХРИ свинца. В референсе есть» → «ХРИ и рентген нуклидов учитывай
в шаблонах нуклидов» → на вопрос «как добавить» оператор выбрал: «делай
полный прогон для метода 1 и добавляй его в вебку. Попутно решай
накопившиеся вопросы».

Механизм К-рентгена дочерних в референсе (Th-232) завязан на MC-шаблоны
`iso_<Nuc>_shield.csv`/`_emitx.csv` по каждому нуклиду цепочки — для
Ra-226 их не было вовсе (метод 1 не строился с самого начала пилота).
Сделано за один заход:

1. `configs/ra226.yaml` дополнен `geant4.gps_region` (та же геометрия,
   что Th-232, скопирована дословно) и `geant4.isotope_runs` (6 звеньев,
   n_events по аналогии с ролью нуклида в цепочке Th-232: Ra226/Rn222=3
   млн как слабый родитель/почти-без-гаммы, Po218/Pb214/Po214=200 тыс,
   Bi214=2 млн как богатейший каскад).
2. `tools/generate_isotope_macro.py` прогнан на новом конфиге →
   `macros/decay_ra226_isotopes.mac` — ПЕРВЫЙ реальный прогон этого
   генератора вообще (для Th-232 он тоже ни разу не гонялся на боевых
   данных, см. аудит Б3 в памяти проекта).
3. Инвокация сверена с `drivers/run_grid.py` (тем же способом
   генерировалась переиспользуемая сетка `grid/rho1.60_E*.csv`, которую
   метод 2 уже использует): `g1s.exe decay_ra226_isotopes.mac vessel 1.60
   OISN16` из `C:\g4work\gamma1s\build`, окружение `. C:\g4work\g4setup.ps1`.
   Прогон в фоне, PID проверен `Get-Process` (инцидент задача 144 в
   истории репо — тишина в консоли не значит смерть процесса).
4. `export_ra226_data.py`: `run_method1()` — копия (не импорт: в
   `export_data.py` `build_templates`/`run_method1` объявлены ВНУТРИ
   `main()`, замыкание на локальные переменные) логики Th-232 —
   `chain_Ra226.csv` (уже был в build/, прежняя работа) задаёт амплитуду
   и форму, `iso_<Nuc>.csv` (новый прогон) — нормированную долю нуклида
   на канал, `iso_<Nuc>_shield.csv` (колонка `src_xray`) — К-рентген
   дочерних отдельной сущностью, с тем же инвариантом баланса
   Σby_nuc==templ_total, что в референсе.
5. `ra226.html`/`ra226.js`: добавлена вкладка «метод 1», попап «как
   посчитано» (у лёгкой страницы попапов не было вовсе) с разделом «Что
   в эту сущность НЕ входит» — та же оговорка про ХРИ свинца защиты, что
   на Th-232 (это и закрывает исходное замечание №5-5). `drawSpectrum`/
   `cursorText`/`attachCursor`/`buildLegend`/`isInert` обобщены
   параметром `mode` ("m1"|"m2") вместо дублирования — риск разъехаться
   с уже пойманным багом makeY/клиппинга (№1, №7 выше) ниже, чем при
   копипасте новых функций.

**Побочная находка при прогоне:** существующий `build/chain_Ra226.csv`
(«прежняя работа», использовался бы как есть) не прошёл сторож
`load_hist()` — посчитан старым exe без поля `src_in_crystal` в шапке.
Пересчитан отдельным макросом (`macros/rerun_chain_ra226.mac`,
дословная копия блока Ra-226 из канонического `decay_all.mac`, 150 тыс.
событий) текущим exe. Без этой проверки страница молча взяла бы
непроверенный файл — тот самый класс дефекта (R61), ради которого
сторож и существует.

**Результат:** A=2884±12 Бк (0,972 паспорта, chi²/ν=8,03), согласуется
с методом 2 (2911/2985 Бк) и паспортом (2967±178 Бк). Доли по нуклидам
физически разумны (проверено вручную): Bi-214 55,5%, Pb-214 36,7%,
K-рентген 5,7%, Ra-226 2,1%, Rn-222 0,04%, Po-218/Po-214 0% (совпадает с
«гамма практически нет» в конфиге). Баланс Σby_nuc==templ_total сошёлся
(инвариант в коде, скрипт не упал). Задеплоено, коммит `450392c`,
запушено.

## 5-7. Заводская калибровка — остаточное смещение, поправка по чистым линиям (10.08.2026) — ЗАКРЫТО

**Замечание:** оператор показал скриншот стороннего ПО (СпектраЛайн,
«Параметры пиков», файл `Ra226_420-7-18`) с пиками около 1763,764/
2203,936/2444,631 кэВ и заявил «калибровка образца не верная. Переделай.
поэтому крайний правый пик и уехал на линию тория 2614. Это ошибка.
Найди причину ошибки и зафиксируй» → «спектралайн. перекалибруй как я
прислал. 2614 на спектре нет» → «в спектралайн не лезь».

Прямая проверка `.spe` (оба файла `-18`/`-19`, одна калибровка прибора)
нашла ось монотонной, без разрывов, но на чистых линиях — остаточное
смещение до −2,6 кэВ (1764,491 кэВ — почти точно; 2204,100 — −2,64).
Отдельно найдено: `ed.FWHM_ANCHORS` (используется по умолчанию
`fit_fwhm_calibration`) — энергии ветви **Th-232** (238,6/583,2/727,3/
860,6/2614,5 кэВ), для Ra-226 там реальных линий нет — деконволюция на
этих точках подгоняла шум/континуум, не физику (объясняет и худшую
точку прежней таблицы ПШПВ: отклонение −35% на 860,6 кэВ). Именно
попытка деконволюции на 2614,5 кэВ (несуществующая для Ra-226 линия) —
вероятный источник путаницы с «линией тория» в постороннем ПО, не в
самой калибровке (СпектраЛайн не трогали, как велено).

**Фикс:** свои чистые изолированные якоря цепочки Ra-226 (без пометки
«+суммирование» в библиотеке) — 186,211/351,932/609,321/1764,491/
2204,100 кэВ. Пробный проход `fit_fwhm_calibration` с этими якорями на
заводской оси даёт истинные центроиды; линейная поправка E=a+b·E по
невязке применяется КО ВСЕМУ спектру до чего-либо ещё (образец, фон,
метод 1, метод 2, ПШПВ-калибровка — вторым проходом уже на поправленной
оси). Коэффициенты прибора (`meta.cal_sample`) не тронуты — это то, что
реально хранит `.spe`; поправка — отдельное прозрачное поле
`meta.energy_correction`, показано на вкладке «калибровка».

Проверена устойчивость наивного взвешенного центроида (линейный фон
между краями окна) — оказалась НЕУСТОЙЧИВОЙ к размеру окна (разброс
несколько кэВ на одной и той же линии при 8/12/16/20 кэВ полуокна) —
отвергнута в пользу деконволюции `fit_peak_multiplet` (та же, что уже
используется по всему проекту для Th-232, методологически надёжнее).

**Результат — независимое подтверждение правильности поправки:**
χ²/ν улучшился у всех вариантов (метод 1 8,03→5,27; метод 2 отобр.
10,50→8,12; метод 2 полн. 6,80→4,42), отношения к паспорту сошлись
теснее (0,972/0,981/1,006 → 1,010/1,027/1,047). СКО невязки якорей
4,3 кэВ (не идеально, но осмысленно для NaI на этой статистике).
Задеплоено, коммит следующим шагом.

## Правило на будущее (по итогам этого разворота)

При портировании визуального элемента «один в один» с референса — после
верстки ОБЯЗАТЕЛЬНО визуальная проверка конкретных пикселей ожидаемого
цвета трассы/элемента, не факта «канва не пустая». Скриншот панели
предпросмотра здесь регулярно не композитит кадры (известная проблема) —
это не повод ослаблять проверку до бессмысленного сравнения, а повод
подобрать точный пиксельный тест (искать цвет `p.ink`/`p.rule`/цвет серии
в конкретных ожидаемых координатах, а не долю «не совпадающих с фоном»).
