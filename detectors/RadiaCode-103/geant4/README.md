# Geant4 — RadiaCode-103

Комплект геометрии RC-103 для Geant4: корпус-envelope, электроника (плата, LiPo,
EMI-ткань, дисплей), детекторный узел (CsI(Tl) 10³ + SiPM + ESR в капсуле).

Полное описание модели, системы координат и провенанса чисел —
[`docs/GEANT4-MODEL.md`](docs/GEANT4-MODEL.md).

**Состав этого репозитория — только то, что написано/сгенерировано и проверено
запуском в этой сессии** (по прямому требованию оператора 26.08.2026: «оставить
только твоё и проверенное», позже уточнено до «оставить только твой комплект, всё
лишнее удалить»). Никаких скопированных-но-не-прогнанных файлов здесь нет.

## Что здесь есть

| Есть | Где |
|---|---|
| SSOT-геометрия — нашёл и исправил в ней реальный баг, провалидировал реальным запуском `CheckOverlaps()` | `gdml/detector/RC103_detector.gdml` |
| Рендер Geant4 в 4 вида (спереди/сбоку/разрез/три четверти) — через общий инструмент `common/tools/gdml_multiview.cc` (грузит GDML напрямую, собран и прогнан в этой сессии) | `verify/RC103_view_{front,side,section,iso}.png`, монтаж `RC103_multiview.png`, сборщик `geometry/assemble_multiview.py` |
| Разрез с размерными линиями (matplotlib, не рендер) | `scripts/dimensioned_section.py` → `verify/RC103_dimensioned_section.png` |
| Журнал инцидентов этой ветки | `audit/project-incidents.md` |
| **Рабочее расчётное приложение** (реальный `BeamOn`, не только рендер) — точечный источник Cs-137, эффективность регистрации, сверено с независимой аналитической оценкой | `run/` (см. `docs/GEANT4-MODEL.md`, раздел «Рабочий расчётный комплект») |

## Статус: GDML провалидирован Geant4 — и рендером, и РЕАЛЬНЫМ `BeamOn` (26.08.2026)

Материал `G4_Si` самоссылался в GDML (копия того же бага, что был в RC-110) —
крашил `G4GDMLParser`. **Исправлено**, мутационно проверено (P-001,
`audit/project-incidents.md`). После фикса прогнан штатный
`G4PVPlacement::CheckOverlaps()` по всей геометрии.

**Важная поправка (P-004, найдена при написании `run/`):** `CheckOverlaps()`
даёт только WARNING даже когда дочерний объём ПОЛНОСТЬЮ вне матери — а
`BeamOn` на это падает фатально. Рендер (использованный для первой валидации)
никогда не доходит до `Initialize()`, поэтому дефект был невидим. 4 позиции
(EMI/окно дисплея/LCD/USB) сдвинуты; мутационно подтверждено лично — на
прежних координатах `BeamOn` падал, на исправленных отрабатывает 1M событий
без единого fatal exception. Урок: «рендер работает» ≠ «расчёты работают» —
приёмка геометрии для MC обязана включать реальный прогон, не только
визуализацию.

## Ограничение платформы

GDML-модуль **есть** в отдельной сборке `C:\geant4-gdml` (статическая, с
Xerces-C++, без OpenGL — тот же тулчейн, что для RC-110). Основная рабочая
сборка `C:\geant4` — по-прежнему без GDML (prebuilt CERN, нет Xerces-C).

## Провенанс

Источник — референсная папка RC-103 оператора (локальная, вне репозитория),
подкаталог `geant4/`, состояние на 2026-08-26. Референсная папка содержит
больше файлов (STL-загрузчики, генератор тесселированных GDML, обмер по
рентгену, первый verify-скрипт) — они прочитаны при разборе, но не собирались
и не запускались в этой сессии, поэтому в этот репозиторий не перенесены.
