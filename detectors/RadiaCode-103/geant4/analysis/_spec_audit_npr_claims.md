Напиши ОДИН файл Python 3.11 `audit_npr_claims.py`. Выведи только код, без пояснений и без markdown-ограждений.

# Назначение
Ярус 0 самоаудита (механический): проверка ЧИСЛОВЫХ и ФАЙЛОВЫХ утверждений документа
`D:\GoogleDrive\Рабочая папка ИИ\GEANT4\references\nonproportionality-csi-tl-concept-2026-09-03.md`
против первичных артефактов. Скрипт НИЧЕГО не «подтверждает по смыслу» — он сверяет числа
и наличие строк. Каждая проверка печатает `OK`/`FAIL` и фактические значения обеих сторон.

# CLI
`python audit_npr_claims.py [--doc <путь.md>] [--selftest]`
Умолчание `--doc` — путь выше. `--selftest` — самопроверка без внешних файлов, код 0/1.
Код возврата основного режима: 0 если все проверки OK, иначе 1 (и в конце сводка «N из M провалено»).

# Проверки

## П1. Таблица кривой CsI(Tl) в документе против CSV
В документе раздел «## 4.» содержит таблицу с колонками `E, кэВ | −40 °C | 0 °C | +40 °C`.
Разобрать её из markdown (строки таблицы начинаются с `|`, десятичный разделитель — ЗАПЯТАЯ,
минус может быть символом U+2212 `−`). Для каждой строки взять E и три значения.
Источник истины: `<каталог скрипта>/out/payne3_fig2_CsI_Tl_{T_m40,T_0,T_p40}.csv`
(колонки `E_keV,rel_light_yield`, строки с `#` — комментарии). Значение при E получать
линейной интерполяцией ПО log10(E). Допуск 0.0006 (документ округлён до 3 знаков после
запятой, полуширина округления 0.0005).
Печатать построчно: E, значение из документа, значение из CSV, разность, вердикт.

## П2. То же для NaI(Tl) — раздел «### 8.2», файлы `payne3_fig2_NaI_Tl_*.csv`.

## П3. Максимумы кривых
Документ утверждает (раздел 4): максимум CsI(Tl) при 0 °C — E = 8,6 кэВ, значение 1,218;
(раздел 8.2) максимум NaI(Tl) при 0 °C — E = 12,7 кэВ, значение 1,162.
Эти два утверждения НЕ парсить из текста, а задать константами в коде с комментарием
«взято из документа». Проверить против CSV: найти строку с максимальным `rel_light_yield`,
сравнить E (допуск 0.05 кэВ) и значение (допуск 0.0006).

## П4. Константы материалов из исходника Geant4
Файл `C:\g4work\thirdparty\geant4-11.2.1\source\materials\src\G4NistMaterialBuilder.cc`.
Проверить, что в нём есть строки, содержащие ровно:
- `AddMaterial("G4_CESIUM_IODIDE", 4.51, 0, 553.1, 2);`
- `AddMaterial("G4_SODIUM_IODIDE", 3.667, 0, 452., 2);`
Печатать номер найденной строки. Если файла нет — вердикт `SKIP` с причиной (не FAIL).

## П5. Пороги продукции из лога прогона
Файл `<каталог скрипта>/../run_field/logs/_chk_origin_on.log`.
Найти блок `Material : G4_Pb` и следующую за ним строку `Energy thresholds`; извлечь
порог для `gamma` и для `e-`. Документ и переписка утверждают: gamma 18,2 кэВ, e- 154,4 кэВ.
Задать эти два числа константами в коде («взято из отчёта»), сравнить с допуском 0.1 кэВ.
То же для `G4_CESIUM_IODIDE`: gamma 6,03 кэВ, e- 94,5 кэВ.

## П6. Результаты прогонов deex
Файлы `<каталог скрипта>/../run_field/output/_deexchk_Bi214_{std,deex,max}.csv`.
Формат: строки `ключ,значение` в шапке (есть `n_events`, `n_hits_in_crystal`, `em_cut_mm`,
`em_deex`), затем пустая строка, затем таблица с шапкой `bin_keV,counts,...`.
Проверить: (а) у всех трёх `n_events` одинаково и равно 100000000; (б) `em_deex` равно
соответственно `std`, `deex`, `max`; (в) `em_cut_mm` = 0.05 у всех.
Затем пересчитать отношения по полосам 20–100 и 100–300 кэВ (bin = кэВ, 1 канал = 1 кэВ,
брать `counts` по индексу канала) и сравнить с утверждениями документа/переписки:
deex/std в 20–100 = 1,001 и в 100–300 = 1,006; max/std = 1,027 и 0,996. Допуск 0.002.

## П7. Наличие файлов, на которые ссылается документ
Проверить существование (не содержимое):
- `D:\GoogleDrive\Рабочая папка ИИ\GEANT4\references\pdf\` — 8 файлов PDF, перечислить имена;
- `D:\GoogleDrive\Дозиметрия\Книги\Гамма-спектрометрия\` — файлы, чьё имя содержит
  `Nonproportionality`, `Khodyuk`, `COSINE`, `BATSE` (ожидается не менее 8 совпадений);
- скрипты `digitize_payne_fig2.py`, `compare_physics_runs.py`, `calib_nonlinear.py`
  в каталоге скрипта.
Печатать фактическое число и список.

## П8. Проверка ссылок вида `файл:строка` из документа
Найти в тексте документа все вхождения шаблона `` `<имя>.cc:<N>` `` и `` `<имя>.hh:<N>` ``.
Для каждого: найти файл по имени рекурсивно от `D:\Claude_files\repos\geant4-detector-models`
и от `C:\g4work\thirdparty\geant4-11.2.1\source`; если найден — проверить, что в нём есть
строка с таким номером (файл длиннее N), и НАПЕЧАТАТЬ саму строку N, чтобы человек мог
сверить смысл. Вердикт `OK`, если файл найден и строка существует; `FAIL`, если файл найден,
но строк меньше N; `SKIP`, если файл не найден.

# Самопроверка `--selftest` (обязана уметь краснеть)
1. Разбор markdown-таблицы: подать строку `| 10 | 1,296 | 1,216 | 1,136 |` — обязано дать
   (10.0, 1.296, 1.216, 1.136). Иначе FAIL.
2. Разбор с минусом U+2212 в шапке и запятыми — обязан работать так же.
3. Мутация сравнения: подать «документное» значение 1.216 против «CSV» 1.226 при допуске
   0.0006 — сравнение ОБЯЗАНО дать FAIL. Если даёт OK — печатать
   `SELFTEST FAIL: сравнение не умеет краснеть` и вернуть 1.
4. Мутация интерполяции: на синтетических точках [(10,1.0),(100,2.0)] интерполяция по
   log10 при E=31.62 обязана дать 1.5 ± 0.01; линейная по E дала бы 1.24 — проверить, что
   результат ближе к 1.5 (то есть используется именно log-интерполяция).
При успехе печатать `SELFTEST OK`, вернуть 0.

# Технические требования
- `# -*- coding: utf-8 -*-`, docstring по-русски, `sys.stdout.reconfigure(encoding="utf-8")`.
- Только стандартная библиотека + numpy. Никаких запросов в сеть.
- Функции: `parse_md_table(lines) -> list[tuple]`, `load_curve(path) -> (np.ndarray, np.ndarray)`,
  `interp_log(E, xs, ys) -> float`, `check(name, got, want, tol) -> bool`,
  каждая проверка отдельной функцией `p1_...`–`p8_...`, `selftest() -> int`, `main(argv) -> int`,
  блок `if __name__ == "__main__": sys.exit(main(sys.argv[1:]))`.
- Вывод: заголовок каждой проверки, строки результатов, в конце сводная таблица
  «проверка | OK | FAIL | SKIP» и итог.
- Все сообщения по-русски. Пути внутри кода — с прямыми слэшами либо через `os.path.join`.
