Сгенерируй ОДИН файл Python 3 — `shield_compare.py`. Ответ: только код, без пояснений и без markdown-ограждения.

# Назначение

Сравнить спектры отклика детектора RadiaCode-103 БЕЗ свинцового домика и С домиком, посчитать коэффициенты ослабления и построить график. Данные — результаты Geant4-прогонов.

# Шапка

`# -*- coding: utf-8 -*-` и docstring: сравнение отклика без домика и с домиком (свинец 50 мм, полость 150x150x385 мм, верх ОТКРЫТ), считает ослабление по энергетическим полосам, строит PNG. Отметить, что открытый верх — главная причина ограниченной эффективности такой защиты, поэтому ослабление в мягкой и жёсткой части сравнивается отдельно.

# Импорты и константы

```python
import os, sys
sys.stdout.reconfigure(encoding="utf-8")
import numpy as np
import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt
```

`HERE = os.path.abspath(os.path.dirname(__file__))`, `sys.path.insert(0, HERE)`, затем `import fit_two_criteria as ftc` — оттуда переиспользуются `read_template` и `rcspec` (НЕ дублировать чтение CSV и свёртку).

`RUN_DIR = os.path.join(HERE, "..", "run_field", "output")`
`OUT_DIR = os.path.join(HERE, "..", "verify")`, создать через `os.makedirs(..., exist_ok=True)`.

Пары для сравнения — список кортежей `(имя, файл_без_домика, файл_с_домиком)`:
`("K-40", "est_K40_noshield.csv", "est_K40_shield.csv")`,
`("Tl-208", "est_Tl208_noshield.csv", "est_Tl208_shield.csv")`.

Полосы: `BANDS = [(20,100),(100,300),(300,700),(700,1500),(1500,2400),(2400,2999)]`.

# Функция `load(fname)`

Строит полный путь в `RUN_DIR`, если файла нет — возвращает `None`. Иначе через `ftc.read_template(path)` получает `(meta, cps, counts)` и возвращает словарь с `meta`, `cps`, `counts`. Сетка 1 кэВ на бин.

# Функция `analyze(name, f_no, f_sh)`

Загружает оба файла. Если хотя бы одного нет — печатает, что данных нет, и возвращает `None`.

Печатает шапку с именем нуклида и таблицу:
- `n_hits_in_crystal` и `cps_total` из `meta` для обоих;
- полное ослабление `atten_total = cps_total(без) / cps_total(с)` — во сколько РАЗ домик ослабил счёт (значение больше 1 = защита работает);
- по каждой полосе из `BANDS`: сумма cps без домика, сумма с домиком, отношение (без/с), и статистическая ошибка отношения по числу отсчётов в полосе (`counts`): `rel = sqrt(1/n1 + 1/n2)`, где n1, n2 — суммы counts в полосе. Если в полосе с домиком меньше 20 отсчётов — пометить строку как ненадёжную явным текстом, не молча.

Возвращает словарь с данными для графика.

# Функция `plot(results)`

Файл `OUT_DIR/RC103_shield_effect.png`. Фигура `plt.figure(figsize=(13, 5.5 * len(results)))`, по две панели на каждый нуклид (`gridspec` с `hspace=0.45`), либо одна строка на нуклид с двумя колонками — на твоё усмотрение, но подписи не должны накладываться.

Для каждого нуклида:
- левая панель: спектры `cps` без домика и с домиком, `ax.set_yscale("log")`, ось X 20..2999 кэВ, легенда ВНЕ поля данных (`loc="center left", bbox_to_anchor=(1.01, 0.5)`) — легенда внутри осей запрещена, она уже перекрывала данные в этом проекте;
- правая панель: коэффициент ослабления по полосам (столбики или ломаная с маркерами), горизонтальная линия на 1.0 пунктиром, подписи полос повёрнуты на 20 градусов, ось Y подписать «во сколько раз ослаблено».

Заголовок фигуры: `RadiaCode-103: влияние свинцового домика (Pb 50 мм, полость 150x150x385 мм, верх ОТКРЫТ)`.

Сохранить с `bbox_inches="tight"`, закрыть фигуру, вернуть путь.

# Функция `main()`

Проходит по парам, собирает результаты, если ни одного нет — печатает, что прогоны ещё не готовы, и завершается через `raise SystemExit`. Иначе строит график и печатает его полный абсолютный путь.

В конце — блок `ТРЕБУЕТ ТОЛКОВАНИЯ`: печатать пункт, если (а) полное ослабление меньше 2 — защита почти не работает, проверить геометрию; (б) ослабление в полосе 20-100 кэВ МЕНЬШЕ, чем в полосе 1500-2400 — это физически подозрительно, мягкие кванты должны гаситься сильнее жёстких; (в) в какой-либо полосе с домиком меньше 20 отсчётов — вывод по этой полосе статистически не обеспечен. Если ни один пункт не сработал — напечатать `(пусто — ни один триггер не сработал)`.

`if __name__ == "__main__": main()`

# Требования

Весь вывод на русском. Комментарии краткие, только там, где объясняют неочевидное. Никаких заглушек.
