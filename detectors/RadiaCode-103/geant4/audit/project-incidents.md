# RadiaCode-103 / geant4/ (GDML-валидация) — журнал ПРОЕКТНЫХ инцидентов

Отдельный журнал именно для ветки `geant4/` (SSOT-геометрия, GDML-рендер, вид/разрез —
аналог того, что для RadiaCode-110 сделано 26.08.2026). НЕ путать с
`detectors/RadiaCode-103/audit/project-incidents.md` — тот журнал ведёт другую,
физическую линию (свинцовая защита, перенос, спектральная декомпозиция), активна и
не закончена отдельно от этой работы; её файл не трогается отсюда во избежание слияния
двух независимых линий правок в одном коммите. Нумерация `P-NNN` здесь СВОЯ, начинается
заново с P-001 (не пересекается с номерами P-NNN того журнала).

Правила ведения: скилл `incident-log`.

---

## Записи

### P-001 — Материал `G4_Si` в GDML самоссылался, крашил `G4GDMLParser`

- **Дата / проект / компонент**: 2026-08-26 / RadiaCode-103 / `geant4/gdml/detector/RC103_detector.gdml`
- **Класс / подкласс**: ПРОЕКТНЫЙ / крах при загрузке (не тихая ошибка — падение)
- **Внешний симптом**: `gdml_vis.exe RC103_detector.gdml ...` завершался кодом
  `-1073741819` (`0xC0000005`, access violation) сразу после чтения блока `<materials>`
- **Воспроизведение**: любая загрузка `RC103_detector.gdml` через `G4GDMLParser::Read()`
  на сборке Geant4 с реальным GDML-модулем (`C:\geant4-gdml`) — до сегодня GDML не
  проверялся штатным парсером вовсе (в основной сборке `C:\geant4` GDML отсутствует)
- **Error verbatim**:
  ```
  *** G4Exception : mat031
        issued by : G4Material::FillVectors()
  For material G4_Si sum of fractional masses 0 is not 1 - results may be wrong
  *** This is just a warning message. ***
  ```
  (варнинг, НЕ фатальный сам по себе — но следом крах `EXIT=-1073741819`, тот же паттерн,
  что и P-001 в RadiaCode-110)
- **Root cause**: `<material Z="14" name="G4_Si"><fraction n="1" ref="G4_Si"/></material>` —
  материал ссылается САМ НА СЕБЯ. Идентичный баг (побайтово тот же XML-паттерн) уже был
  найден и исправлен в `detectors/RadiaCode-110/geant4/gdml/detector/RC110_detector.gdml`
  (P-001 того журнала) в ходе ЭТОЙ ЖЕ сессии, но раньше по времени. Вероятно, RC-103 GDML
  был создан по шаблону RC-110 ДО того, как там баг нашли — копия унаследовала дефект.
- **Как пойман**: мутационный тест (не гадание) — `gdml_vis.exe` прогнан на файле ДО
  правки (крах подтверждён) и ПОСЛЕ (успех подтверждён), см. «Проверка» ниже.
- **Затронутые данные**: до этой правки `RC103_detector.gdml` НИКОГДА не проверялся
  штатным Geant4-парсером (GDML-модуль появился в проекте только сегодня) — то есть
  файл существовал невалидированным с момента создания.
- **Исправление**: локальное переопределение `<material name="G4_Si">` удалено (заменено
  комментарием), `materialref="G4_Si"` на `SiPM_log` резолвится через NIST-фоллбэк
  `G4GDMLReadMaterials::GetMaterial()` — та же логика, что подтверждена по исходнику
  Geant4 при разборе P-001 RC-110 (см. `detectors/RadiaCode-110/audit/project-incidents.md`).
  Правка внесена в референсный файл
  `D:\GoogleDrive\Дозиметрия\Спектры\RC103\geant4\gdml\detector\RC103_detector.gdml`
  (SSOT) и синхронно скопирована в этот репозиторий.
- **Проверка исправления (#SA-3, мутационно)**: `gdml_vis.exe` на файле ДО правки —
  `EXIT=-1073741819` (крах); ПОСЛЕ правки — `EXIT=0`, полный успешный разбор геометрии,
  включая последующий overlap-check по всем 20 physvol (см. ниже) и запись PNG.
- **Сканирование класса**: остальные 9 материалов файла (`G4_AIR`, `G4_CESIUM_IODIDE`,
  `ESR_foil`, `Case_ABS`, `PCB_FR4`, `LiPo_602560`, `LCD_stack`, `Display_window_PC`,
  `USB_connector`, `EMI_ESD_fabric`, `SiPM_carrier`) прочитаны построчно — ни один не
  ссылается сам на себя во `<fraction ref="...">`.
- **Owner / status**: GEANT4 / **CLOSED-fixed**

---

### P-002 — Число треугольников STL в документации/комментариях было неверным (два разных случая)

- **Дата / проект / компонент**: 2026-08-26 / RadiaCode-103 / `docs/GEANT4-MODEL.md`,
  `cadmesh/RC103CADMeshCase.hh`, `cadmesh/RC103HybridExample.cc`
- **Класс / подкласс**: ПРОЕКТНЫЙ / документация не соответствует данным (не крашится,
  но вводит в заблуждение)
- **Внешний симптом**: оператор попросил перепроверить `verify/` целиком («нужно всё
  проверять») — при чтении файлов, скопированных без правки, найдено число «2792
  треугольника» в трёх местах (`GEANT4-MODEL.md` дважды, `RC103CADMeshCase.hh`), и
  отдельно «110666 triangles» в `RC103HybridExample.cc`.
- **Root cause**: проверено фактом (не по памяти) — прочитан STL-заголовок и размер
  файла на диске: `Rc-103.stl.bak` = **18516** треугольников (925884 байт,
  `84 + 18516×50 = 925884` — совпадает), `Rc-103.stl` = **102252** треугольника
  (5112684 байт, аналогично совпадает). Ни один реальный файл не даёт 2792 —
  число выдумано или взято из промежуточного/несуществующего состояния. «110666» —
  это число треугольников STL **RC-110** (`Rc-110.stl`), скопированное в комментарий
  RC-103 по шаблону и не пересчитанное.
- **Как пойман**: по прямому требованию оператора перепроверить `verify/` — при
  сверке нашёл расхождение между `docs/case_removed_log.md` (925884 байт для
  `.bak`) и `GEANT4-MODEL.md`/`cadmesh`-комментарием (2792 tri, что при 84+n×50
  дало бы 139684 байт, НЕ 925884) — числа физически противоречили друг другу в
  двух местах одного и того же файла, что и вывело на проверку fact-based.
- **Затронутые данные**: только комментарии/документация — сама геометрия (GDML) и
  расчётный код (`RC103CADMeshCase.cc`) числа треугольников не используют, влияния
  на физику/результаты нет.
- **Исправление**: `GEANT4-MODEL.md` (таблица «В корне RC103», таблица «Три варианта
  корпуса»), `RC103CADMeshCase.hh`, `RC103HybridExample.cc` — заменены на измеренные
  числа с пометкой «проверено по факту 26.08.2026», добавлено явное указание, что
  `stl_to_gdml.py::resolve_stl()` берёт `Rc-103.stl` (102252 tri) первым, `.bak`
  (18516 tri) — только fallback.
- **Проверка исправления**: числа в правке — прямой вывод `struct.unpack_from("<I",
  data, 80)` на обоих файлах, не переоценка «на глаз».
- **Сканирование класса**: другие числовые утверждения о STL в этой ветке (только
  `README.md`/`GEANT4-MODEL.md`/`cadmesh/*`) проверены при этой же читке — других
  расхождений о числе треугольников не найдено.
- **Owner / status**: GEANT4 / **CLOSED-fixed**

### P-003 — `verify_align.py`: легенда перекрывает данные, USB/EMI не рисуются вовсе

- **Дата / проект / компонент**: 2026-08-26 / RadiaCode-103 / `scripts/verify_align.py`
- **Класс / подкласс**: ПРОЕКТНЫЙ / визуальный дефект генератора верификационной
  картинки (не баг геометрии — числа в `rc103_coords.py` корректны)
- **Внешний симптом**: оператор посмотрел опубликованный `RC103_align_check.png` и
  спросил «а этот разве не с ошибкой?» — на 2D-разрезе плата (PCB) визуально
  обрывается заметно раньше правого края корпуса, как будто геометрия неполная.
- **Воспроизведение**: `python verify_align.py` → `verify/RC103_align_check.png`,
  смотреть 2D-панель «разрез Y=0» в правом верхнем углу.
- **Root cause**: `ax.legend(loc="upper right", fontsize=8)` (без `bbox_to_anchor`,
  без явной прозрачности) рисуется ВНУТРИ осей поверх данных — matplotlib-легенда
  с непрозрачным фоном по умолчанию закрывает правую треть графика (правый край
  корпуса, платы, аккумулятора, полости). Подтверждено пиксельным анализом:
  зелёная полоса платы (PCB) реально доходит почти до правого края корпуса
  (x-диапазон 959-1681 px против корпуса 948-1681 px в системе изображения),
  просто последние ~250px скрыты под легендой. Отдельно, независимо от легенды:
  функция `draw_xz_section()` вообще не рисует `USB_TypeC` и `EMI_ESD_fabric`,
  хотя оба есть в `COORDS` (`rc103_coords.py`) и в актуальном GDML — не баг
  чисел, а неполный набор отрисовываемых компонентов.
- **Как пойман**: не поверил на слово собственной первой проверке («скопировано —
  значит нормально») — по прямому вопросу оператора провёл пиксельный анализ
  (найти по цвету границы прямоугольников корпуса/платы, сравнить диапазоны) вместо
  повторного визуального впечатления, затем прочитал исходник скрипта построчно.
- **Затронутые данные**: только сам PNG (визуализация) — `rc103_coords.py` и GDML
  не менялись, использованы верно.
- **Исправление**: НЕ ВНЕСЕНО в код скрипта (оставлено как есть — оператор
  попросил вместо починки просто убрать непроверенный артефакт из `verify/`
  репозитория, см. коммит 26.08.2026). Известный баг задокументирован в
  `docs/GEANT4-MODEL.md` («Известный баг verify_align.py»), `RC103_align_check.png`
  удалён из git-репозитория (остаётся в референсной папке оператора как есть).
- **Проверка исправления**: неприменимо — фикс не вносился, только удаление из
  публикации.
- **Сканирование класса**: тот же класс («легенда/аннотация matplotlib перекрывает
  данные без bbox_to_anchor») в `scripts/dimensioned_section.py` и
  `scripts/xray_measure.py` (annotate()) — проверено визуально при читке в этой же
  сессии, легенда/подписи там не перекрывают значимые элементы (легенда
  `dimensioned_section.py` — в нижнем правом углу свободной области, подтверждено
  просмотром итогового PNG).
- **Owner / status**: GEANT4 / **OPEN** (сам скрипт не починен; митигировано
  удалением из публикации)

---

## Overlap-валидация (`G4PVPlacement::CheckOverlaps`, 26.08.2026) — НЕ инцидент, справочная запись

После фикса P-001 файл впервые прогнан через штатный оverlap-checker Geant4
(`fParser.SetOverlapCheck(true)` в `common/tools/gdml_vis.cc`, тот же механизм, что
использует физическая линия RC-103 для P-025 её журнала). Полный лог — ниже. Каждая
находка классифицирована: **фон** (уже задокументированное в `docs/GEANT4-MODEL.md`
упрощение геометрии-примитива, протрузия — прямое следствие этого упрощения, правка НЕ
вносилась) или **малый артефакт** (геометрия построения ESR-плёнки/EMI, величина в
пределах десятков микрон, не искажает физику, оставлено как есть).

| Объект | Протыкает | Величина | Классификация |
|---|---|---|---|
| `SiPM_log` | мать `Capsule_cavity_log` (−Z) | 400 мкм | фон: ниша SiPM Ø11×2.2мм не смоделирована (`GEANT4-MODEL.md`, «Не моделируется») |
| `SiPM_carrier_log` | сосед `pv_capsule_shell` | 425 мкм | фон: тот же узел (ниша), капсула — box вместо формы с нишей |
| `Display_window_log` | мать `Case_interior_log` (−Z) | 1.3 мм | фон: окно физически в толще передней стенки корпуса, вырез в ABS не смоделирован («без окна в ABS») |
| `Display_LCD_log` | мать `Case_interior_log` (−Z) | 600 мкм | фон: тот же узел, что окно дисплея |
| `EMI_log` | мать `Case_interior_log` (+Z) | 50 мкм | малый артефакт: `coords.py` (z=7.15±0.15) даёт край чуть за границу полости (7.25); blender-bbox той же ткани (`verify/blender_bbox_rc103-2.json`) даёт край 7.275 — тоже за границей, на схожую величину, то есть это не опечатка в одном источнике, а мелкое реальное превышение в обоих независимых источниках |
| `USB_log` | мать `Case_interior_log` (+X) | 1.75 мм | фон: корпус без выреза под разъём («без USB-выреза») |
| `pv_pcb` (сосед `DetectorModule_log`) | сосед `DetectorModule_log` | 485 мкм | фон: плата — монолитный бокс без U-образного выреза под капсулу («PCB без U-cutout») |
| `ESR_y_log` / `ESR_z_log` | сосед `pv_esr_px` (по 2 раза каждый) | 12-32 мкм | малый артефакт: 5 граней ESR-плёнки (10.13×10.13, на 0.065 мм шире грани кристалла с каждой стороны — умышленный нахлёст для light-tightness по образцу реальной обёртки) слегка перекрываются в углах куба |

**⚠ ПОПРАВКА (P-004, см. ниже): вывод «не требующими правки» ниже был НЕПОЛНЫМ.** Он
верен для рендера/визуализации (`CheckOverlaps()` даёт только WARNING, рендер никогда
не запускает навигацию/transport). Но для РЕАЛЬНОГО `BeamOn` четыре из перечисленных
protrusions (`Display_window_log`, `Display_LCD_log`, `USB_log`, а через них
транзитивно и попытка `Initialize()`) оказались ФАТАЛЬНЫМИ — `G4SmartVoxelHeader::
BuildNodes()` падает намертво, когда дочерний physvol ПОЛНОСТЬЮ (не частично) лежит
вне материнского объёма. Обнаружено и исправлено в P-004 — координаты этих трёх
компонентов в GDML СДВИНУТЫ (не оставлены как задокументированное упрощение). Таблица
ниже — исторический снимок ДО этого фикса, оставлена для провенанса; актуальные
позиции — в самом GDML-файле.

**Вывод (устарел для BeamOn, см. поправку выше; верен для рендера)**: после фикса
P-001 модель геометрически состоятельна там, где заявлена (корпус/полость/капсула/
кристалл без конфликтов друг с другом); все найденные overlaps объясняются уже
ЗАРАНЕЕ задокументированными упрощениями примитивов (ниша/вырезы не моделируются)
либо являются суб-100-микронными артефактами построения, не требующими правки.
Численные значения добавлены в `docs/GEANT4-MODEL.md` («Ограничения модели») —
до этой проверки ограничения были описаны качественно, без количественной меры.

---

### P-004 — Реальный `BeamOn` фатально крашился: `CheckOverlaps()` не ловит «дочерний объём ПОЛНОСТЬЮ вне матери»

- **Дата / проект / компонент**: 2026-08-26 (поздний вечер) / RadiaCode-103 /
  `gdml/detector/RC103_detector.gdml` (позиции `p_emi`, `p_disp_win`, `p_disp_lcd`,
  `p_usb`), написание рабочего Geant4-приложения `geant4/run/` по требованию оператора
  («должен быть полный актуальный комплект... с обвязкой и хотя бы одним прогнанным
  тестовым расчётом»).
- **Класс / подкласс**: ПРОЕКТНЫЙ / крах при инициализации (не при чтении GDML — при
  `G4RunManager::Initialize()`, ПОЗЖЕ, чем любая из сегодняшних предыдущих проверок
  доходила).
- **Внешний симптом**: `rc103_run.exe` (новое приложение, честный `BeamOn`, не только
  рендер) падал с `-1073740791` (`0xC0000409`) сразу после блока overlap-check, до
  первого события.
- **Воспроизведение**: сборка `run/` на НЕТРОНУТОМ (на тот момент) SSOT
  `RC103_detector.gdml`, запуск `rc103_run.exe <gdml> 1000000 <out.csv>`.
- **Error verbatim**:
  ```
  *** G4Exception : GeomMgt0002
        issued by : G4SmartVoxelHeader::BuildNodes()
  PANIC! - Overlapping daughter with mother volume.
           Daughter physical volume pv_display_window
           is entirely outside mother logical volume Case_interior_log !!
  *** Fatal Exception *** core dump ***
  ```
- **Root cause**: `G4PVPlacement::CheckOverlaps()` (использован сегодня раньше, см.
  «Overlap-валидация» выше) при `Construct()` печатает WARNING для ЛЮБОГО overlap,
  включая случай «дочерний объём ПОЛНОСТЬЮ вне материнского» — не блокирует
  дальнейшую работу. Но `G4SmartVoxelHeader::BuildNodes()` (вызывается ПОЗЖЕ, при
  `RunManager::Initialize()`, строит навигационную структуру для трекинга частиц)
  на ТОТ ЖЕ случай — дочерний объём целиком вне матери — падает ФАТАЛЬНО, не
  WARNING. Рендер (`gdml_vis.cc`/`gdml_multiview.cc`, все сегодняшние проверки этого
  утра) никогда не вызывает `Initialize()`/`BeamOn` — только `Construct()` +
  отрисовку, поэтому дефект был структурно невидим всем сегодняшним проверкам ДО
  попытки реального расчёта. Виноваты 4 позиции: `p_disp_win` (окно дисплея
  ПОЛНОСТЬЮ вне `Case_interior_log` по Z — не частично, как ошибочно
  классифицировано в «Overlap-валидации» выше), `p_disp_lcd`, `p_usb`, `p_emi`
  (частичные протрузии тех же родом, тоже задеты правкой для консистентности).
- **Как пойман**: фоновым агентом при выполнении задачи «подготовь рабочий счётный
  комплект» — агент честно НЕ ограничился рендером/`CheckOverlaps()`, довёл дело до
  реального `BeamOn`, наткнулся на крах, локализовал причину арифметически по
  самому GDML (не гадал), сделал diagnostic-копию для изоляции гипотезы и проверки
  остального стека (не трогая защищённый SSOT), доложил находку честно вместо того
  чтобы скрыть/обойти.
- **Затронутые данные**: МОЙ СОБСТВЕННЫЙ более ранний вывод («Overlap-валидация»
  выше, «ни одна протрузия не потребовала правки координат») — оказался НЕПОЛНЫМ:
  верен для рендера, неверен для расчётов. Явно помечено поправкой в тексте выше,
  не переписано молча.
- **Исправление**: координаты `p_emi`, `p_disp_win`, `p_disp_lcd`, `p_usb` в
  `RC103_detector.gdml` сдвинуты внутрь `Case_cavity` с запасом ~0.10 мм на сторону
  (минимальный сдвиг, устраняющий именно фатальный случай «полностью вне», не
  идеальное физическое решение — см. комментарий P-004 прямо в GDML рядом с
  позициями, там же альтернативы, которые НЕ выбраны: вырез в `Case_shell`,
  расширение `Case_cavity`). Компромисс: USB теперь не выходит за пределы
  внутренней полости вовсе (раньше хотя бы физически «торчал наружу», что ближе к
  реальности разъёма) — сознательная жертва точностью ради работоспособности
  расчётов, отмечено как компромисс, не как улучшение.
- **Проверка исправления (#SA-3, мутационно, лично, не со слов агента)**: сама
  запустила `rc103_run.exe` на ИСПРАВЛЕННОМ официальном SSOT (не diagnostic-копии)
  — `EXIT=0`, 1 000 000 событий, никакого `PANIC`/fatal exception в логе (только
  WARNING-и по остаточным малым overlaps — `SiPM`/`SiPM_carrier`/`ESR`/`PCB`/
  `Display_LCD↔window`/`USB↔PCB`, все частичные, не «entirely outside», не крашат).
  Числа (`n_hits=107764`, `n_photopeak=25025` на 1M событий) побитово совпали с
  прогоном на diagnostic-копии агента — консистентно (EMI/Display/USB физически
  далеко от кристалла, сдвиг на них transport к кристаллу не задевает).
- **Сканирование класса**: остальные overlaps из таблицы «Overlap-валидации» (SiPM
  400 мкм, SiPM_carrier 425 мкм, PCB 485 мкм, ESR 12-32 мкм) — ЧАСТИЧНЫЕ протрузии
  (не «entirely outside»), подтверждено тем же прогоном на исправленном SSOT: они
  остались как WARNING, `BeamOn` их не крашит. Класс закрыт именно для случая
  «полностью вне» — частичные overlaps остаются задокументированным упрощением, как
  и было (это НЕ ошибка прежней классификации, только «полностью вне» была ошибкой).
- **Независимая стерильная проверка (§31.A, сделана ДО коммита):** отдельный
  фоновый агент, не видевший моих чисел/выводов, — (а) с нуля пересчитал
  арифметику геометрии источника ПРЯМО по GDML (координаты кристалла, наружной
  грани, телесный угол конуса) — совпало с ранее написанным кодом независимо;
  (б) собрал приложение в СВОЁМ отдельном build-каталоге (не переиспользуя мои
  объектные файлы); (в) прогнал 1M событий на официальном исправленном SSOT в
  СВОЙ CSV — `EXIT=0`, ни одного fatal exception, только non-fatal WARNING
  (частичные overlaps, ожидаемо); (г) независимо посчитал аналитическую оценку
  (свой NIST-запрос, своя цитата) — 2.213×10⁻⁴ против его же MC 2.050×10⁻⁴
  (расхождение ~8%, жёстче требуемого «в пределах порядка величины»). Числа
  совпали с моим прогоном на официальном SSOT побитово (`n_hits=107764`,
  `n_photopeak=25025`) — не пересказ, независимое воспроизведение.
- **Owner / status**: GEANT4 / **CLOSED-fixed** (мутационно проверено лично на
  официальном SSOT + независимо стерильным агентом, оба ДО публикации).

---

### P-005 — Посадка прибора в домике: расчёт кладёт прибор ПЛАШМЯ в центр полости, реальная постановка не описана

**Когда:** 2026-08-29, при проектировании калькулятора укрытия.
**Статус:** OPEN — нужен ответ оператора о реальной постановке.

**⚠ Первая редакция этой записи была НЕВЕРНА** (числа «кристалл на 167,5 мм над дном»,
«реально 76,0», «расхождение 91,5»). Она исходила из того, что прибор стоит
вертикально в сосуде и что начало координат модели — центр кристалла. Оператор
указал на ошибку, факты перепроверены по GDML и коду; ниже — пересчёт.

**Что показывают факты.**

- `gdml/detector/RC103_detector.gdml:127` — `Case_outer x="123.0" y="34.0" z="17.5"`:
  длинная ось прибора в модели идёт вдоль **X**, толщина 17,5 мм — вдоль **Z**.
  То есть в домике прибор **лежит плашмя**, а не стоит.
- `gdml/detector/RC103_detector.gdml:17` — `p_detector x="-49.5" y="0" z="-0.55"`:
  кристалл смещён от центра габарита прибора на 49,5 мм вдоль длинной оси.
- `run_field/Rc103FieldDetectorConstruction.cc:272` — прибор ставится в `(0,0,0)`,
  то есть центром ГАБАРИТА в центр полости по X и Y.
- `run_field/Rc103FieldDetectorConstruction.cc:94-95` — полость z от −167,5 до
  +217,5 мм (дно 50 мм, верх открыт), x и y от −75 до +75.
- Сосуда в `run_field` нет вовсе — считается голый прибор в домике.

**Отсюда положение кристалла в расчёте:**

| Величина | Значение |
|---|---|
| Над дном полости | **166,95 мм** (167,5 − 0,55) |
| До открытого среза верха | **218,05 мм** |
| Смещение от оси полости вдоль X | **49,5 мм** |
| До ближней боковой стенки | **25,5 мм** (при 124,5 мм до дальней) |

Последнее — самостоятельная особенность, которую стоит знать: кристалл сидит не
на оси домика, а вплотную к одной из стенок, вчетверо ближе, чем к противоположной.

**Реальная постановка (ответ оператора 29–30.08):** прибор лежал **плашмя, экраном
вверх, на картонной коробке 25 мм** над дном полости. Отсюда пересчёт:

| | расчёт 29.08 | реально | разница |
|---|---|---|---|
| Кристалл над дном полости | 166,95 мм | **34,30 мм** | **132,65 мм** |
| Кристалл до открытого среза верха | 218,05 мм | 350,70 мм | |
| Телесный угол открытого верха | 0,3995 ср (6,36 % полусферы) | 0,1704 ср (2,71 %) | **×2,35** |

Расчёт: `analysis/solid_angle_top.py` (контура GEANT4), формула прямоугольного проёма
из точки со смещением 49,5 мм от оси. Оценка ГЕОМЕТРИЧЕСКАЯ — доле счёта через верх
не равна, рассеяние в стенках в неё не входит.

**Второе отличие — ориентация.** `p_disp_win z=-7.00` (GDML): в модели прибор лежит
**экраном ВНИЗ**, реально — экраном ВВЕРХ. Реальная постановка есть модель,
развёрнутая на 180° вокруг длинной оси. Следствие не только в 1,1 мм положения
кристалла: меняется, что лежит НАД кристаллом со стороны прихода излучения через
открытый верх (58,1 % счёта). В модели сверху батарея (`p_battery z=+3.5`) и
EMI-экран (`p_emi z=+7.0`); в реальности сверху дисплей, SiPM и плата, а батарея
снизу. Для мягкой части спектра это разный материал на пути.

**В чём собственно дефект.** Постановка расчёта (прибор плашмя габаритным центром в
центре полости, экраном вниз, без опоры) — допущение из `run_field/_spec_shield.md`,
принятое ради вложения домика в сферу источника R_SRC = 300 мм. Оно НЕ совпадает с
реальным измерением 23 суток ни по высоте (132,65 мм), ни по ориентации (180°), и
сравнение модель/измерение опубликовано 29.08 без этой оговорки.

**Связь с известной аномалией.** Модель видит открытого неба в 2,35 раза больше
реального, то есть должна ПЕРЕбирать счёт, приходящий сверху. Опубликованное
превышение модели в жёсткой части (1,18–1,42), где преобладает космика через
открытый верх, совпадает с этим по направлению. Недобор в мягкой части (0,79–0,93)
этим НЕ объясняется — там причина другая. Гипотеза; проверяется прогоном.

**Почему поймано только сейчас.** Гейты проверяли вместимость прибора в полость и
вложенность домика в сферу источника — обе проходят при любом положении внутри.
Вопрос «а так ли он лежит у оператора» не задавал никто; ответ занял одну реплику.

**Корректирующие меры:**
1. Посадка становится параметром CLI (`stand=<мм>`, ориентация), а не константой кода.
2. Посадка и ориентация пишутся в шапку выходного CSV наравне с прочими признаками.
3. `rsrc` — параметр: при низкой посадке дальний верхний угол домика уходит за 300 мм.
4. **Пересчитать якорь** в реальной постановке (плашмя, экраном вверх, опора 25 мм)
   и сравнить с опубликованным — этап Ш1.
5. Гейт: постановка, сравниваемая с измерением, обязана нести явный список ВСЕХ
   отличий от реального эксперимента, включая ориентацию прибора и опору.

**Урок процесса (мой, не проекта).** Дважды подряд утверждал числа, выведенные из
непроверенной посылки об ориентации прибора, — сначала «стоит вертикально в сосуде»,
затем не проверив, какой стороной он лежит. Оба раза ответ лежал в GDML в одной
строке `define`. Класс: непроверенная посылка внутри формально корректной цепочки;
арифметическая самопроверка её не ловит, ловит только чтение исходника — и вопрос
оператору о том, чего в исходнике быть не может.

**Owner / status:** GEANT4 / **ОТКРЫТ ЗАНОВО** — посадка исправлена в коде и
пересчитана, но результат НЕ подтвердил ожидание, а породил новую находку
(см. ниже). CLOSED объявлять преждевременно.

### Пересчёт якоря в реальной посадке (30.08.2026, полные данные)

Все 8 нуклидов + мюонный шаблон 5·10⁸ (5 порций по 1e8, сложены
`merge_muon_runs.py`, дополненным проверкой посадки) пересчитаны с
`stand=25 flip=up`. `predict_shield.py` дополнен: читает `stand_mm`/
`screen_up` из шапки CSV, печатает постановку явно, FATAL при смешении
шаблонов с разной посадкой (мутационно проверено). **Регрессия на СТАРЫХ
файлах воспроизвела опубликованные 29.08 числа побитово** (0,925/0,874/
0,788/1,071/1,178/1,244/1,416, χ²/n.d.f.=23,473) — сам скрипт корректен.

**Сравнение старая посадка → реальная посадка (модель/измерение по полосам):**

| полоса, кэВ | старая (опубл. 29.08) | реальная (30.08) |
|---|---|---|
| 20–100 | 0,925 | 0,433 |
| 100–300 | 0,874 | 0,401 |
| 300–700 | 0,788 | 0,517 |
| 700–1500 | 1,071 | 0,866 |
| 1500–2000 | 1,178 | 1,059 |
| 2000–2400 | 1,244 | 1,136 |
| 2400–2830 | 1,416 | 1,259 |
| **χ²/n.d.f.** | **23,47** | **196,13** |

**Гипотеза P-005 подтвердилась только ЧАСТИЧНО.** В жёстких полосах
превышение действительно СНИЗИЛОСЬ (1,178→1,059, 1,244→1,136, 1,416→1,259) —
направление совпадает с расчётным телесным углом (модель видела неба в 2,35
раза больше). Но в мягких полосах, где старая посадка давала приемлемое
согласие (0,79–0,93), реальная посадка даёт **тяжёлый недобор (0,40–0,52)** —
это НЕ было видно раньше и НЕ предсказывалось гипотезой. χ²/n.d.f. вырос
на порядок (23,47 → 196,13): реальная посадка физически честнее, но
согласия с измерением стало ХУЖЕ, а не лучше.

**Разбивка по происхождению счёта (факт из CSV, не домысел)** показывает,
что доля «мимо свинца» упала у всех нуклидов (K-40 55,4%→45,6%, Tl-208
55,2%→44,0%, у остальных на 4–9 п.п.), но упала МЕНЬШЕ, чем упал общий счёт
(в 2,0–2,3 раза) — то есть доля «рассеяно в свинце» выросла (K-40 29,6%→
38,1%, Tl-208 26,8%→34,6%). Мюонный шаблон с домиком почти не изменился
(46151→43581 попаданий на 5e8, −5,6%) — резко контрастирует с гамма (−57 до
−59%): космика приходит преимущественно вертикально, её поток через
открытый верх слабо зависит от высоты кристалла внутри полости, в отличие
от изотропного гамма-поля.

**ПРИЧИНА недобора в мягкой части НЕ установлена.** Кандидаты (не проверены,
перечислены как гипотезы для дальнейшей работы, не как выводы):
рассеяние в стенках/полу свинца эффективнее переносит мягкие фотоны к
кристаллу, чем учитывает простая геометрическая картина; поле `wf_room_*`
(нормировка через тождество Коши, независимая от положения приёмника)
может неточно представлять реальное поле вблизи пола домика; необнаруженный
источник мягкого счёта (радон в воздухе полости — раздел проекта, шаблон
«радон в воздухе помещения» по-прежнему не считался).

### Проверка гипотезы "радон в воздухе полости" (30.08.2026) — ОТВЕРГНУТА при типичных концентрациях

По прямому указанию оператора посчитан вклад: новое приложение `run_shieldair`
(детекторная геометрия ПЕРЕИСПОЛЬЗОВАНА из run_field — §33, не копия), источник —
Pb-214/Bi-214 (гамма-активные дочери Rn-222), ион равномерно по объёму воздуха
полости (150x150x385 мм, ~1% занимает прибор — отбраковка не сделана, оценка
первого порядка). 5·10^6 событий на нуклид, посадка реальная (P-005).

**Результат: вклад НИЧТОЖЕН.** При 30 Бк/м3 — 0,24% от наблюдаемого дефицита
(0,171745 имп/с в окне 20-2830 кэВ), при 50 Бк/м3 — 0,41%. Для ПОЛНОГО объяснения
дефицита нужна концентрация ~12300 Бк/м3 — на порядки выше любого правдоподобного
значения в помещении или в закрытой полости без экстремального источника.

**Гипотеза "радон в воздухе полости при типичной концентрации" ОТВЕРГНУТА** как
объяснение широкополосного дефицита. Сигнал из сканирования мюонов (Ra226_chain
хочет амплитуду в разы больше известной) остаётся неотвергнутым, но воздух полости
— не его источник; кандидаты остаются те же (рассеяние в свинце эффективнее
модели, систематика нормировки wf_room, либо что-то ещё не рассмотренное).

**Действие:** оператору доложено ДО публикации (в отличие от рецидива
W-039). Не объявлять линию закрытой, не публиковать страницу как
«валидированную», пока недобор не разобран либо явно принят оператором как
открытый вопрос.

### Свободная подгонка в домике без опоры на активность (30.08.2026, по указанию оператора)

Новый скрипт `analysis/plot_shield_free.py`: NNLS-подгонка (K40, Ra226_chain,
Th232_chain, mu) прямо на измерение в домике, активности открытого фона НЕ
используются вообще — только форма измеренного спектра. Постановка (P-005)
считана из шапки шаблонов и подтверждена (25 мм, экран вверх).

chi2/ndf = 17,04 (против 196,13 у предсказания с фиксированными активностями),
невязка формы = 0,0618. Подогнанные амплитуды: K40 = 23,5 ± 97,6 (не
определена), **Ra226_chain = 144,1 ± 17,9 Бк/кг (в 5,3 раза больше известных
27,1)**, Th232_chain = 15,7 ± 9,4 (согласуется с известными 14,9), мюоны =
400,8 ± 17,6 с⁻¹ (0,94 от PDG). Подтверждает находку `scan_mu_fit.py`: избыток
формы избирательно приписывается именно Ra226_chain, Th232_chain не задет.

**Мера W-класса (единицы):** первая версия скрипта делила компоненты на
`live_s` ВТОРОЙ раз (они уже были в имп/с после `A_s @ amp`, так как подгонка
шла в отсчётах через `A_s*live_s`) — все закрашенные области графика
оказались практически нулевыми, а нижняя панель (модель/измерение) — плоской
линией на уровне ~0. Поймано визуально по факту (не должно быть настолько
"пусто"), не проверкой числа. Исправлено в том же ходе.

### Проверка гипотезы "ошибка счёта ослабления свинцом" (30.08.2026, по вопросу оператора) — ЧАСТИЧНО ОТВЕРГНУТА

Проверены два конкретных под-варианта:

1. **Избирательная ошибка в РАСЧЁТЕ переноса** (баг в физике/геометрии,
   действующий именно на энергиях Ra226_chain). Диагностика:
   `analysis/decompose_shield.py`, T(E) = (шаблон с домиком)/(шаблон без
   домика), отдельно по каждому из 8 нуклидов, реальная посадка. Кривые
   T(E) для Pb-214/Bi-214 (цепочка Ra-226) гладкие и по величине
   сопоставимы с K-40/Ac-228/Bi-212/Tl-208 на тех же энергиях — ни излома,
   ни выброса, ни аномального превышения именно на линиях Ra226_chain.
   График: `verify/RC103_shield_decomposition.png`. **Признаков
   избирательного бага в переносе НЕ найдено.**
2. **Толщина свинца в модели не соответствует реальной.** `kShieldPbMm =
   50.0` (`Rc103FieldDetectorConstruction.hh:82`) — зашитая constexpr,
   ДО этого момента ни разу не сверенная с реальным домиком оператора.
   **Оператор подтвердил фактом ("точно"): реальная толщина стенок —
   ровно 50 мм, без зазоров.** Кандидат ОТВЕРГНУТ.

**Вывод:** ослабление свинцом как таковое (ни расчёт, ни геометрия толщины)
не объясняет находку. Причина недобора мягкой части остаётся ОТКРЫТОЙ;
кандидаты сузились до: систематика нормировки `wf_room_*` вблизи пола
домика; эффективность рассеяния в свинце (форма углового/энергетического
распределения, не толщина); реальная неоднородность активности вблизи пола
против допущения "изотропное поле по всей комнате", на котором стоит метод
поле→отклик.

### Проверка формы (не только доли) рассеянной/рождённой компоненты (30.08.2026, по указанию "копни")

Углубление п.1 выше: не только ЧИСЛО событий по происхождению, а форма
энергетического распределения. Посчитано (одноразовый разбор CSV, скрипт не
сохранён — тривиальная ad-hoc агрегация, воспроизводима из тех же файлов):

- Доля «рассеяно+рождено в Pb» в полосе дефицита 20–700 кэВ растёт
  МОНОТОННО с типичной энергией линий нуклида одинаково для ОБЕИХ
  цепочек: Ra-226 (186 кэВ) 23,6 % → Pb-214 (242-352) 27,9 % → Ac-228
  (338-969) 35,7 % → Pb-212 (238) 24,6 % → Bi-212 (727-1620) 38,4 % →
  Bi-214 (609-2204) 45,3 % → Tl-208 (583-2614) 49,6 % → K-40 (1461) 49,0 %.
  Один общий физический тренд, не два разных по цепочкам.
- Форма спектра рассеянной+рождённой компоненты по каналам (сумма по
  Ra226_chain против суммы по Th232_chain, картинка
  `verify/RC103_origin_shape_check.png`) визуально ПОЧТИ ИДЕНТИЧНА между
  цепочками: тот же двойной пик ~75–90 кэВ, тот же спадающий континуум.

Попутно чуть не принял артефакт за баг: пик 75–90 кэВ есть и в категории
«прямых» (никогда не заходивших в свинец домика) — совпадает по
положению с К-флуоресценцией Pb (Kα 73/75, Kβ 85/87 кэВ). Проверено:
в GDML самого прибора свинца/вольфрама НЕТ (`grep` по материалам —
пусто), значит это не утечка классификации из корпуса. Рабочая гипотеза
(НЕ проверена дальше, не факт) — совпадение с пиком обратного рассеяния
от СТЕН КОМНАТЫ (расчёт по формуле Комптона на 180° для линий 186–352 кэВ
даёт 90–125 кэВ, что близко к наблюдаемому диапазону) — известный в
гамма-спектрометрии эффект перекрытия двух пиков в этой области. Не
чинилось: не факт, что это баг, а не физика, и он одинаков в обеих
цепочках — не объясняет асимметрию находки.

**Итог по «копни»:** ни грубая доля, ни форма энергетического
распределения scat/born-компоненты не показывают ничего специфичного
именно для Ra226_chain. Кандидат 2 (эффективность рассеяния в свинце)
остаётся неподтверждённым на обеих проверках.

### Статус на конец дня 30.08.2026 — оператор идей не имеет, дальше не копаем

Проверены и закрыты (отвергнуты фактом или диагностикой) три кандидата:
радон в воздухе полости (§ выше, отвергнут при типичных концентрациях);
ошибка расчёта ослабления свинцом (T(E) гладкая, без аномалий); толщина
свинца ≠ модельной (оператор подтвердил факт 50 мм). Кандидат 2
(эффективность/форма рассеяния) проверен глубже по указанию "копни" —
тоже не подтверждён. **Причина широкополосного дефицита мягкой части
(вывод свободной подгонки: Ra226_chain хочет ×5,3 от известной активности)
ОСТАЁТСЯ НЕУСТАНОВЛЕННОЙ.** Оставшиеся, ещё не проверенные кандидаты:
систематика нормировки `wf_room_*` вблизи пола домика; реальная
неоднородность активности вблизи места установки домика (допущение
изотропного поля по всей комнате может не выполняться локально).

По прямому указанию оператора линия НЕ продолжается дальше сегодня
("идей нет"). Не объявлять причину найденной, не публиковать текст
страницы с утверждением о полном согласии.

---

### P-026 — 2026-09-03 — `rc103_field.exe` упал с 0xC0000005 на событии 0, повтор не воспроизвёл

**Что случилось.** Первый рабочий прогон `rc103_field.exe <wf_room_Bi214.csv> 100000000 ...
stand=25 flip=up seed=4242 deex=std` завершился через 0,2 мин кодом
`-1073741819` (access violation) сразу после строки `--> Event 0 starts.`; CSV не
записан, `.err` пуст. Дымовые прогоны той же сборки на 2000 и 10⁶ историй с теми же
ключами прошли (проверены обе переменные: путь вывода и N). Повторный запуск ТОЙ ЖЕ
команды (отвязанным процессом) дошёл до `EXITCODE=0`, 27 021 попаданий, как и два
следующих прогона (`deex`, `max`) — падение не воспроизведено ни разу из трёх.

**Что известно / не известно.** Сборка 03.09 (новые ключи `emcut=`/`deex=`,
`G4EmParameters` после конструктора option4) — единственная переменная относительно
прогонов 29–30.08, но дымовые и три рабочих прогона на той же сборке чистые. Дампа
памяти нет, `.err` пуст; отличить разовый сбой (память/антивирус/сон диска
GoogleDrive) от редкой гонки в коде невозможно без дампа.

**Мера.** (1) Правило прогона: любой прогон `run_field` считается состоявшимся
только по маркеру `EXITCODE=0` в логе И наличию CSV; Monitor обязан различать
«DIED» и «FINISHED» (сделано 03.09). (2) При повторе — включить генерацию дампа
(`WER LocalDumps` для `rc103_field.exe`) до разбора, код не править по догадке
(#DBG-1). (3) Класс: сканировать логи `run_field/logs/*.err` на непустые файлы
при каждой серии.

**Owner / status:** GEANT4 / **OPEN-not-reproduced** (наблюдение зафиксировано,
причина не установлена; правок кода нет).

### P-027 — 2026-09-11 — шаблон Ti44chain (смесь AmTiCsEu, Гамма-1С) складывал Ti-44 и Sc-44 в одно событие

**Что случилось.** Группа Ti44chain считалась прогоном цепочки Z 20–22 одним событием
(`g1s_npsm.exe primary=ion chain=44:44:20:22`, скрипт контура `run_mix_templates.sh`). В
`g1s_npsm` нет временного окна (греп `GlobalTime` — 0 совпадений), поэтому γ Ti-44 и излучение
Sc-44 (T½ 4 ч) складывались в одно событие. Признаки: нефизический пик 78,3 + 1157 = 1235 кэВ
(бин 5015 отсчётов против соседей около 150); пики 67,9 / 511 / 1157 кэВ шаблона ниже
прямого отклика на 29–43 %.

**Последствия.** Путь 1 разбора смеси давал Am-241 1,641 к паспорту при χ²/ν 305: подгонка
добирала недостачу Ti в зоне 50–70 кэВ амплитудой Am.

**Мера.** Раздельные прогоны Ti44only (Z 22) и Sc44only (Z 21) по 10⁷ распадов, столбец —
их сумма (`build/Gamma-1S-npsm-1142/out/mix_Ti44split_npsmoff.csv`). Выгрузка
`detectors/Gamma-1S/web-th232/export_amticseu_data.py` берёт его через `tpl()`. После
поправки: бин 1235,5 — 5015 → 78; путь 1 Am 1,475, Ti 0,947, χ²/ν 195,8.

**Скан класса.** Cs137chain (Cs-137 → Ba-137m, T½ 2,55 мин) тоже считался одним событием.
Измерено сравнением с прямым γ-откликом 661,657 кэВ: выше пика 1,5·10⁻⁶ на распад против
1,86·10⁻² в пике — пренебрежимо. Am241 и Eu152 — один нуклид. Шаблоны Th-232 / Ra-226 —
проверить отдельно. Правило: если промежуточное звено цепочки живёт много дольше, чем
собирается событие, цепочку одним событием не считать — только раздельные прогоны,
сложенные в равновесии.

**Owner / status:** GEANT4 / **FIXED** для смеси AmTiCsEu; класс по Th-232 / Ra-226 — OPEN.

### P-028 — 2026-09-11 — метод 2 смеси AmTiCsEu: F_B-депопуляция выедала каскадные линии на 90–97 %

**Что случилось.** Прогоны сетки отклика `g1s_npsm` (`build/Gamma-1S-npsm-1142/out/grid_mar_E*.csv`)
пишут в бин 0,5 кэВ события, где квант не отложил энергии в кристалле: 90–94 % событий,
гистограмма в сумме ровно 1,0 на квант. `erd.run_method2` берёт полную эффективность
партнёра каскада как `shape.sum()` готовой формы, поэтому получал ≈ 1 вместо 0,06–0,10.
Депопуляция: Ti-44 67,9 / 78,3 кэВ — 96 / 92 %; Eu-152 121,8 / 244,7 / 411 / 779 / 867 / 1090 /
1299 кэВ — 90–97 %. Недостающий счёт в зоне 50–100 кэВ подгонка отдавала америцию.

**Последствия.** Метод 2: χ²/ν 865, Am-241 2,45 к паспорту, Ti 1,50 — против пути 1
1,50 / 0,96.

**Как пойман.** Сравнением столбцов метода 2 с шаблонами пути 1 на распад по полосам
энергий: Ti в полосе 50–100 кэВ 0,162; Eu 0,20–0,65 почти во всём диапазоне. Затем прямой
просмотр нижних бинов гистограмм сетки. Прежняя проверка («столбцы совпадают в пределах
1–6 %») смотрела пики, а не полосы, и дефект не видела.

**Мера.** `detectors/Gamma-1S/web-th232/export_amticseu_data.py`, `grid_response` и прогон пары
Sc-44: бины < 1 кэВ отбрасываются до размытия, при сумме > 0,6 на квант — громкий отказ.
**Проверка меры:** на старом поведении сумма 1,0 → отказ срабатывает; после правки
депопуляция 4,6–8,8 %, столбцы на распад 0,94–1,03 (Ti) и 0,78–1,12 (Eu), метод 2:
χ²/ν 280,9, Am 1,458 / Ti 0,953 / Cs 1,448 / Eu 1,001 против пути 1 1,500 / 0,959 / 1,319 / 0,993.

**Скан класса.** Страницы Th-232 и Ra-226 строят отклик по старой сетке `rho1.60_E*.csv`
(`export_data.make_full_response`); в опубликованных данных Ra-226 депопуляция не больше 6,9 %
(`src/data-ra226.js`) — бина нулевого отложения там нет. Новая сетка `g1s_npsm` используется
только выгрузкой смеси. Остальные потребители `read_template` с суммой по всей гистограмме —
проверить.

**Owner / status:** GEANT4 / **FIXED**.

### P-029 — 2026-09-11 — шкала пробы рвалась на первом репере (59,5 кэВ, пик Am-241); таблица LSRM нумерует каналы с 1

**Что случилось.** (1) `detectors/Gamma-1S/analysis/mix_unfold_g1s.py`, `recalibrate_energy`: ниже
первого репера шла квадратичная экстраполяция по ВСЕМ реперам, а через первый репер этот многочлен
не проходит: канал 25,455999 → 61,27 кэВ, 25,456 → 59,54 кэВ; шкала немонотонна, каналы 25–26 на
24–28 % уже соседних, свёртка в энергии занижала модель на пике Am-241. Дефект был с первой
генерации функции и пережил D-018 / W-078. (2) Таблица пиков LSRM (`position_ch`) и многочлен шкалы
файла заданы в нумерации с 1, а массив отсчётов — с 0: центры 5 симметричных изолированных пиков
(Cs-137, Mn-54, Na-22, Co-60 ×2) по индексу массива на 1,0 ± 0,15 кан ниже `position_ch`. Шкала
пробы стояла на канал (~3 кэВ) не там.

**Как пойман.** Стерильный самоаудит 11.09: проход Б (разрыв), проход А (нумерация). Тест
`test_recal_extrap.py` зелёный на обоих дефектах — он проверял каналы от репера, но не сам репер.

**Мера.** (1) Та же квадратичная форма, сдвинутая к первому реперу (непрерывно); тест пары
c0−ε / c0 на обоих крайних реперах (на старом коде — 61,27 / 59,54, красный). (2) Параметр
`ch_offset` (`recalibrate_energy`, `core.unfold`), в конфиге смеси `fit.lsrm_ch_offset: 1.0`.
**Проверка меры:** путь 1, окно 25–1500: исправленная шкала без сдвига χ²/ν 287, со сдвигом 171.

**Скан класса.** Читалка `.spe` — SpectraVibe (чужая зона): у них `channel_to_energy` применяется к
индексу с 0 (`dead_time.py:114`), соглашение о нумерации таблицы LSRM не оговорено — письмо
владельцу. Страницы Th-232 / Ra-226 используют ту же читалку — проверить.

**Owner / status:** GEANT4 / **FIXED** для смеси; класс (SpectraVibe, Th-232/Ra-226) — OPEN.

### P-030 — 2026-09-15 — `read_template` открывал шаблон без `encoding`: путь с «И» в шапке ронял подгонку

**Что случилось.** `detectors/Gamma-1S/analysis/mix_unfold_g1s.py:118` — `open(path, 'r')` без кодировки. Python на
машине оператора читает в cp1251, байт 0x98 (вторая половина UTF-8 «И») в cp1251 не определён → `UnicodeDecodeError`.
Шаблоны Geant4 пишутся в UTF-8; до 15.09 в шапках не было путей с «И», дефект был латентным. Режим
`primary=gamma_table` (дыра 26к) пишет в шапку `spectrum_csv` — путь `…Рабочая папка ИИ…` сделал его активным.

**Как пойман.** Субагент части C (стенд `add_ib_template.py`) заметил `open` без encoding при чтении кода и поставил
отказ кодом 3; оркестратор подтвердил на живых файлах: 4 тестовых шаблона `k24t_*` падали с `can't decode byte 0x98`.

**Мера.** `encoding='utf-8'` в `:118`. **Проверка меры:** хеши вывода `read_template` по всем шаблонам `out/`: до правки
567 читаются, 4 отказа; после — 567 совпали побитово, 0 расхождений, 4 прежних отказа читаются. Все 571 файл
декодируются UTF-8 без ошибок (108 содержат не-ASCII).

**Скан класса (15.09).** `grep -rnE "open\("` по `detectors/Gamma-1S/**/*.py` без `encoding`/`rb`/`wb`: 15 строк.
Многострочные вызовы `cell_lsrm_curve.py:83`, `mix_unfold_g1s.py:164`, `build_page.py:221/223`, `export_data.py:333/976`
(обёртка `io_open` = `encoding="utf-8"`, `:359-360`) — кодировка задана. Без кодировки остаются только чтения числа
телесного угла (`compare_point.py:71`, `export_curves.py:145`, `point_recalc.py:371`, `tcc_selfcheck.py:170/196`,
`zones_scan.py:83`) — файлы из одного ASCII-числа, пишутся нашими драйверами; риск не активен, не правлено. Контур:
`GEANT4\scripts\mix_unfold_g1s.py:79` — старая копия с тем же дефектом, но все 6 потребителей (`fit_kit2024`,
`cal0_kit2024`, `light_scale_kit2024`, `probe_scale_indep`, `recount_shielding_indep`, `_gen_light_scale_kit2024_r2`)
ставят `…/Gamma-1S/analysis` первым в `sys.path` → копия не импортируется (мёртвый дубль; удаление — по команде).
Режим `gamma_table` в любом случае получает таблицы по ASCII-пути (`build/…/ib_tables/`).

**Owner / status:** GEANT4 / **FIXED**.

