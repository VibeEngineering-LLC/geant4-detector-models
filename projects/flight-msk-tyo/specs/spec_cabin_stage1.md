You are a C++17 engineer working with Geant4 11.4.2 (MSVC). Write ONE complete source file `cabin_stage1.cc` (contains `main`). Output ONLY the code (no markdown fences, no prose). Comments in Russian, short.

## Purpose
Stage I of a two-stage cosmic-ray simulation in an aircraft cabin: primary particles of ONE component (e.g. neutron) are fired from a sphere of radius R around the cabin sector according to PARMA tables; every particle ENTERING the recorder volume "Tube" is written to a binary phase-space file. One process = one component = one output file.

## Available project headers (already written, use them, do not re-implement)
* `parma_source.hh`: `class ParmaSource { bool Load(const std::string& path, std::string& err); double TotalFlux() const; ... }` (flux in cm^-2 s^-1).
* `parma_primary.hh`: `class ParmaPrimary : public G4VUserPrimaryGeneratorAction { ParmaPrimary(const ParmaSource* src, const std::string& particle_name, double radius_cm); }` — throws `std::runtime_error` for an unknown particle name.
* `cabin_geom.hh`: `struct CabinParams { double tubeX, tubeY, tubeZ, tubeR, tubeHalfL; ... }` and `class CabinDet : public G4VUserDetectorConstruction { const G4LogicalVolume* TubeLV() const; const CabinParams& Params() const; }`.
* `psp_record.hh`: `struct PspRec {int32_t evt; int32_t pdg; float E_MeV, phi, y_cm, ux, uy, uz, t_ns, Ep_MeV, wp; int16_t proc, vol, ip, pad;}`, `int ProcCode(const std::string&)`, `int VolCode(const std::string&)`, `class PspWriter { bool Open(const std::string&); void Write(const PspRec&); void Close(); long long Count() const; }`.
* `capture_step.hh` + `capture_emitter.hh`: `class CaptureEmitter { bool Load(const std::string& path, std::string& err); }` and `class CaptureStep : public G4UserSteppingAction { explicit CaptureStep(const CaptureEmitter*); void UserSteppingAction(const G4Step*) override; long long nReplaced, nFallback, nNoIon, nIonGround; }` (replaces the gamma cascade of neutron capture with a database cascade; it uses the protected member `fpSteppingManager` of `G4UserSteppingAction`, so its `SetSteppingManagerPointer` must be forwarded, see below).

## Command line
`cabin_stage1 <table.tab> <particle> <ip> <N> <seed> <out.psp> [capture_db]`
`particle` = Geant4 name (neutron, proton, mu+, mu-, e-, e+, gamma); `ip` = component index 0..6 written into every record; `N` = number of events (long long); `seed` = long long. Fewer than 7 arguments or more than 8 -> usage message on stderr, `return 2`. The sphere radius is the constant `kSphereR_cm = 520.0`.

## Program structure
1. Static global context struct `Ctx` holding: `ParmaSource src`, `int ip`, `PspWriter writer`, `const CabinDet* det`, `double Ep_MeV`, `double wp` (primary energy and z direction cosine of the current event), `long long nCross`.
2. Physics list: `G4PhysListFactory factory; auto* pl = factory.GetReferencePhysList("FTFP_BERT_HP");` (check non-null, else message and `return 2`); `pl->ReplacePhysics(new G4EmStandardPhysics_option4());` `pl->RegisterPhysics(new G4RadioactiveDecayPhysics());` `pl->SetDefaultCutValue(1.0 * mm);`.
3. Run manager: `G4RunManagerFactory::CreateRunManager(G4RunManagerType::Serial)`; `SetUserInitialization(det)` with `new CabinDet()`, then `SetUserInitialization(pl)`; `G4Random::setTheSeed(seed)` BEFORE `Initialize()`.
4. After `Initialize()` register the actions: `new ParmaPrimary(&g_ctx.src, particle, kSphereR_cm)` (catch `std::runtime_error`: message on stderr, `return 2`); a tracking action; a stepping action.
5. Tracking action `class Trk : public G4UserTrackingAction`: in `PreUserTrackingAction(const G4Track* t)`, if `t->GetParentID() == 0` store `g_ctx.Ep_MeV = t->GetKineticEnergy()/MeV` and `g_ctx.wp = t->GetMomentumDirection().z()`.
6. Stepping action `class Stp : public G4UserSteppingAction` with a pointer member `CaptureStep* cap` (may be nullptr):
   * override `SetSteppingManagerPointer(G4SteppingManager* p)`: call `G4UserSteppingAction::SetSteppingManagerPointer(p);` and, if `cap`, `cap->SetSteppingManagerPointer(p);`.
   * `UserSteppingAction(const G4Step* s)`: first `if (cap) cap->UserSteppingAction(s);`. Then recording: `pre = s->GetPreStepPoint()`; return unless `pre->GetStepStatus() == fGeomBoundary` and `pre->GetPhysicalVolume() != nullptr` and `pre->GetPhysicalVolume()->GetLogicalVolume() == g_ctx.det->TubeLV()`. Get the track `trk = s->GetTrack()`; return if `trk->GetDefinition()->GetParticleName()` starts with "nu_" or "anti_nu_"; return if it is a gamma with kinetic energy < 0.010 MeV. Fill a `PspRec r`: `evt` = `G4EventManager::GetEventManager()->GetConstCurrentEvent()->GetEventID()`; `pdg` = `trk->GetDefinition()->GetPDGEncoding()`; `E_MeV` = `pre->GetKineticEnergy()/MeV`; with `pos = pre->GetPosition()/cm` and the tube centre `(P.tubeX, P.tubeZ)` from `g_ctx.det->Params()`: `phi = atan2(pos.z() - P.tubeZ, pos.x() - P.tubeX)`, `y_cm = pos.y() - P.tubeY`; `ux, uy, uz` = `pre->GetMomentumDirection()`; `t_ns = pre->GetGlobalTime()/ns`; `Ep_MeV`, `wp` from the context; `proc` = `ProcCode(cp ? cp->GetProcessName() : "")` where `cp = trk->GetCreatorProcess()`; `vol` = `VolCode(lv ? lv->GetName() : "")` where `lv = trk->GetLogicalVolumeAtVertex()`; `ip` = `(int16_t) g_ctx.ip`; `pad = 0`. `g_ctx.writer.Write(r); ++g_ctx.nCross;`.
7. Main: load the table with `g_ctx.src.Load(argv[1], err)` (on failure message + `return 2`); open the writer (failure -> message + `return 2`); if an 8th argument is given, create `static CaptureEmitter emitter`, `Load` it (failure -> message + `return 2`), create `CaptureStep` and pass it to `Stp` — the emitter must outlive the run. Construct the physics/geometry, then apply these UI commands after `Initialize()` and PRINT the returned status of each to stdout as `UI <command> -> <status>`: `/process/had/rdm/thresholdForVeryLongDecayTime 1.0e+5 s`, `/run/verbose 0`, `/event/verbose 0`, `/tracking/verbose 0`.
8. Run `runManager->BeamOn((G4int) N)`. Then close the writer and write a text file `<out.psp>.meta` (plain ASCII, `key=value` per line): `particle`, `ip`, `N`, `seed`, `R_cm`, `flux_cm2s` (`TotalFlux()`), `T_sim_s` = `N / (flux * pi * R_cm^2)`, `crossings` (`g_ctx.nCross`), `psp_records` (`writer.Count()`), and if capture is active `cap_replaced`, `cap_fallback`, `cap_no_ion`, `cap_ion_ground`. Also print one line `STAGE1 events=<N> crossings=<n> T_sim_s=<x>` to stdout. Return 0 only if `writer.Count() == g_ctx.nCross`, else print an error and return 3.

## Requirements
* Include every header used: G4RunManagerFactory, G4PhysListFactory, G4VModularPhysicsList, G4EmStandardPhysics_option4, G4RadioactiveDecayPhysics, G4UserTrackingAction, G4UserSteppingAction, G4Step, G4StepPoint, G4Track, G4EventManager, G4Event, G4LogicalVolume, G4VPhysicalVolume, G4VProcess, G4UImanager, G4SystemOfUnits, Randomize.hh, plus the project headers listed above, plus <cmath>, <cstdio>, <fstream>, <iostream>, <string>, <stdexcept>.
* No `using namespace std` in headers; free functions and classes at file scope are fine.
* `M_PI` needs `_USE_MATH_DEFINES` — define your own `constexpr double kPi = 3.14159265358979323846;` instead.
* Compile with MSVC `cl /EHsc /std:c++17 /utf-8` against Geant4 11.4.2 headers.
